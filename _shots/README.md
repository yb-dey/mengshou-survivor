# 全界面巡检截图（art-shots 流水线自动落盘）

推这个目录下任何改动即触发；产物同时写 ci/out 与 _shots/v1210/。

## ⚠ 新鲜度规矩（2026-09-24 实测踩到，写在最前面）

**截图不会随代码自动更新。** 触发条件只有 `_shots/**` 与 workflow 文件本身
（`.github/workflows/art-shots.yml` 的 `on.push.paths`）——**改了游戏再 push 不会刷新它们**。
实测：v1.210m/n 四个提交之后，`v1210/` 里仍是那四个提交**之前**的画面，
而当时的巡检照样打印「产物三形态同源 ✓」——**版本串同源 ≠ 你看到的画是同源**。

⇒ 判新鲜度只看一件事：**`_shots/v1210/*.png` 的时间 vs 母版最后一次改动的时间**。
`_qc/_audit-omissions.mjs` 已把这条加进巡检（截图落后于母版即报 STALE）。

## 怎么刷新

1. push 任意改动到本目录（这份 README 也算）→ 云端 `art-shots` 跑一遍，把新图 + `run.txt`
   **提交回仓库**；也可走 `workflow_dispatch` 手动触发。
2. `ci/out/` 被 `.gitignore` 忽略 ⇒ workflow 里必须 `git add -f`，否则提交阶段「绿着但啥也没提」（已修）。
3. 新图落地后本地重建看板：`node _qc/_shots-gallery.mjs`。

## ⚠ 派生视图**天然落后一批**（设计如此，不是故障）

`v1210-phone/`（`_qc/_phone-view.mjs` 从 `v1210/` 缩出来的真机视图）与 `gallery.html` 都是**派生件**：
它们的更新本身也算一次 `_shots/**` push ⇒ 又会触发下一轮重拍。所以派生件永远比最新一批**早一批**，
这是循环，不是缺陷。**判断"能不能拿它下结论"看的是：这一批与母版之间有没有改到它承载的东西** ——
真机视图承载的是**文字/尺寸**，若那一轮只改了地面贴图，它就仍然有效。

## 刷新记录（每次重拍写一行：这批画到底对应哪一版）

| 时间(UTC) | 触发原因 | 对应母版 | 结果 |
|---|---|---|---|
| 2026-09-24 13:02 | v1.210 系列收尾 | v1.210s | 13 张 720×1280 ✓ |
| 2026-09-24 14:40 | **v1.210t 装备袋改程序化剪影**（战场上的袋子不再是木箱）+ **v1.210u 帮助页字号 17/18/19→21px** —— 这两改**动了画面/文字**，13:02 那批已不代表当前版本 | v1.210u `cca8e03` | **已落地 `400f26e`（14:37）**：13 张，其中 10 张有变化；派生件（`v1210-phone/` + `gallery.html`）随后本地重建 ✓ |
| 2026-09-24 15:00 | **v1.210v 设置页字阶**：操作提示 17→18px、「音频三档」小签 14→18px（**改了文字 ⇒ 08-settings 必须重出**） | v1.210v `dafe589` | **已落地 `ac6feaa`（15:03）**：08-settings 已重出；真机直看 18px 未裁切 ✓ |
| 2026-09-24 15:20 | **v1.210w 加菜面板两句提示 15/14→18px**（`_qc/_type-candidates.mjs` 筛出的可抬站点里先做掉的 2 个） | v1.210w `033c7cc` | **已落地 `1874f92`（15:26）**：10-daily 已重出；1:1 直看两句 18px **未裁切、未压返回钮** ✓ |
| 2026-09-24 15:40 | **v1.210x 承诺守卫 + 承诺句前提注释**（只改注释与 dist/deploy，**不动任何像素**） | v1.210x | 重拍后应与本批图逐字节相同 ⇒ art-shots 可能报"无产物变化"（**这是对的，不是失败**） |
| 2026-09-25 01:20 | **v1.211a 大厅主题签状态字 11→15px**（运行时实测挑出：它是"必须动手"的短词里最小的，1.04mm） | v1.211a | 本次 push 触发重拍（**01-home 会变**） |
| 2026-09-24 17:47 | **v1.211b 大厅地面草簇"看得见"**（`paintHallGround` 草簇 s 14..26→**50..62** 画布 px、边距 48→88）—— 真机簇高 **0.70..1.30mm → 2.56..3.08mm**，是**尺度账**不是口味：旧值连项目自己的 2.5mm 可读线都没进过（同族：字号那条线）。判据来自新量具 `_qc/_imgstat.mjs`（阴性对照 11/11）实测大厅 y800-960 段 cf **18.9** / edgePct **4.00%**，而同一屏自己的 CTA 段 cf **84.7**。**动了画面 ⇒ 01-home 必须重出** | v1.211b | **已落地 `a6e7f37`（17:49）**：`01-home` 467719→469439 字节。`_qc/_band-diff.mjs` 实测目标段 **3160 px 变化（2.74%、平均Δ86.7）**，y0-160/y1120-1280 **0 变化**。⚠ 但 cf 18.9→19.3、edgePct 4.00%→4.42% ⇒ **尺寸修好了、密度远远不够**（7 簇/瓦 ⇒ 整块地板仅 ≈15 簇） |
| 2026-09-24 17:52 | **v1.211c 草簇密度 7→16 簇/瓦**（同一轮内闭环）：尺寸已进可读区间，只补数量 ⇒ 整屏 ≈34 簇 | v1.211c `35f710b` | **已落地 `de56022`（17:54）**：01-home 469439→482918 字节。判据（`_qc/_imgstat.mjs --bands 8`，对**同一 `--bands` 口径**比三批）：目标段 y800-960 的 **edgePct 4.00% → 4.42% → 7.73%**、edge 1.15→1.25→**2.09**、cf 18.9→19.3→20.7、uniq 749→760→788、近灰 12.8%→10.1%；y960-1120 edgePct 9.69→9.72→**9.83**。**无副作用**：y0-160 与 y1120-1280 三批全等（14.30% / 16.25%），CTA 段 y480-640 **20.83→21.00（没被拉低）**、cf 84.7（保住）。⚠ cf 不作局部判据（整幅离散度，对局部改动不敏感）|
| 2026-09-24 18:12 | **11-battle 拍法改了（`ci/screen-sweep.mjs`，第 126 轮）**：原来钉死在"进战斗后第 7 秒"—— 那个秒数是为了给 12-levelup 攒一个升级弹窗，**不是为拍战斗** ⇒ 拍到的是空场。现改为轮询到真的有怪（`enemyCount ≥ 10`，上限 60s）才拍。依据：`_qc/_imgstat.mjs` 量出 11-battle 的 edgePct **4.37% 是 13 屏最低**，而它是玩家花 90% 时间的一屏；且它连「独特色数/主色占比」都是 `-`（那段量只写在 STEPS 循环里）| 第 126 轮 `de91ab9` | **已落地 `3ff8613`（18:05）**。云端注解实证：`11-battle 采样密度 同屏怪=10 峰值=10 目标=10 轮询=63 结束=达到 10 只`；`sweep` 结论 **success**（12/13 两屏没被带崩）。报告里 11-battle 首次有读数：**独特色数 724 · 主色占比 21.9%**，备注列「同屏怪 **10** 只」；12-levelup 697/32.9%、13-pause 709/32.4% 不再是 `-`。`_imgstat` 量：**edgePct 4.37%→6.55%、edge 1.33→1.96、cf 61.7→69.3、lumSd 35.1→40.0**；`01-home` 逐项不变（本轮未动游戏代码）|
| 2026-09-24 18:22 | **11-battle 取"本次最密帧"（`ci/screen-sweep.mjs`）**：上一版在**没命中阈值时**照样按"循环结束那一刻"拍 ⇒ 云端实测拍到过 **0 只怪**的帧（比我原本要修的"第 7 秒空场"还空），且密度标签记的是瞬时值、**描述的不是这张图**。现改为怪数刷新新高就立刻覆盖一张，标签改报**这张图自己的**怪数 | 第 126 轮 `cd4aee0` | 本次 push 触发重拍。**三批实测（`_imgstat`）**：原第 7 秒固定帧 edgePct **4.37%** → 命中阈值那次 **6.55%** → 超时拍到 0 只那次 **4.08%（比我修的问题更差）**。验收：本批注解应出现「这张图同屏怪≥1」，且 edgePct 不再低于 4.37% |
| 2026-09-25 01:5x | **把"地面重复"这件事量成两个频段（`_qc/_tile-repeat.mjs` 加带通，对照 11/11）** —— 起因是我在新 11-battle 图上看见"一圈圈按网格重复的浅环"，而母版 v1.210g/h/m 已为同一现象调过三轮（**属有意设计，不许凭观感判死刑**）| 第 127 轮（只动 `_qc/` 与本文件，**未动游戏代码**）| **实测（带通，低频=σ12 模糊；高频=原图−低频）**：战场地面 y300-1150 **低频 3.66 vs 高频 1.95 ⇒ 低频占主导**，与肉眼看见的"环"一致；大厅地板 y900-1060 v1.211a **2.56/0.48（5.3×，典型壁纸）→** v1.211c **4.31/3.76（1.15×）** ⇒ v1.211b/c 那笔加草**把低频占比从 84% 压到 53%**（加的是质感不是壁纸）。⇒ **结论：两处都不要改**。⚠ 以后凡论"重复"必须分频段说：`amp` 混报会把"加草"读成"加壁纸" |
| 2026-09-25 02:0x | **补两屏"每个玩家每局都会看到、却从来没被拍过"的：结算（胜/负）** ⇒ 巡检 13 屏 → **15 屏**。发现方式不是"看着不对"，而是**数一数哪些屏根本没被拍过**：13 屏里没有结算页，而 `win()`/`lose()`/`resultBuild()` 三个钩子母版（L39150/L39155/L39015）早就给了却没人用 | 第 128 轮（`ci/screen-sweep.mjs`，**未动游戏代码**）| 本次 push 触发重拍。**验收**：`_shots/v1210/` 应出现 **14-result-win.png / 15-result-lose.png**；报告里这两行的「实测状态」必须是 `RESULT_WIN`/`RESULT_LOSE`（写错会进 dupList ⇒ sweep 退 2，**会红不会静默**），备注列出现「结算卡带 N 条 · 压住=否」。⚠ `chips` 字段名我第一版按印象写错（母版实际是 **`chipN`**），会静默变 null —— 已按源码改正 |
| 2026-09-25 02:2x | **v1.211d 修结算页头行被削平**（`drawResultPanel` 画序错：章题条画在文字**之后**，把 16px 字上沿切掉 6px = 字身 37.5%）。**这一屏本轮才第一次被拍（13→15 屏），第一次看就看见了** | v1.211d | 本次 push 触发重拍。**验收**：`14-result-win` / `15-result-lose` 的**头行字顶不得再有横向切口**（`node _qc/_crop.mjs <图> 200 22 340 70 4` 放大复核）；条子占 py+2..py+24、16px 字上沿 py+24 ⇒ **重叠 0px**。⚠ 顺带证实：**"把没拍过的屏补进巡检" + "能放大看细节"** 这两件事是发现它的直接原因 |
| 2026-09-25 03:0x | **补拍仓库「天赋」页签**（`05b-vault-talent`）—— 由新检查 `_qc/_shot-coverage.mjs` 查出：05-vault 拍的是默认 craft 页，**talent 页从来没有截图**。判据不是 `vault===true`（craft 页同样为真 = 空门），而是给 `state()` 新增的 `vaultTab` 字段做**值比对**（巡检新增 `VALUES` 表） | 第 129 轮 `v1.211e` | 本次 push 触发重拍。**验收**：出现 `_shots/v1210/05b-vault-talent.png`；报告该行「实测状态」应为 `HOME` 且断言通过（`vaultTab=talent`）；注解应打印 `05b-vault-talent … vaultTab=talent`。⚠ 同时把 `want` 里的死条目 `openPetCard`（母版没有这个钩子）删掉 |
| 2026-09-25 03:3x | **补拍「进场过场」（`11a-entrance`）** —— 每个玩家每局开头都会看到，此前从没被拍过。发现路径：修完 v1.211d（章题条压住文字上沿）后先查**同族版式**，`drawChapterMapStrip` 全文件只有两处调用，另一处就是这里；逐行读过画序确认那处**没问题**（条子在自己的 `clip()` 里、且在文字之前），但顺势发现**这一屏根本没被拍过** | 第 130 轮 `v1.211f` | 本次 push 触发重拍。**验收**：出现 `_shots/v1210/11a-entrance.png`；注解 `11a-entrance 采样 过场 t=…/… kind=chapter first=…`；报告该行备注列带**采样时刻**。断言 `live===true` 不通过会 `::error::` + sweep 退 2。⚠ **覆盖检查查不到它**（时间是驱动、无钩子可开）⇒ 已给母版补 `enterCine()` 探针并写进 `want`；**"有钩子的屏" ≠ "所有屏"** 这句已写进 `_shot-coverage.mjs` 文件头 |
| 2026-09-25 04:0x | **补拍「复活盘」+「怎么玩帮助页」，并把 4 处缺失的状态断言补齐** —— 判据从"钩子名"换成**游戏自己声明的 `GAME.flow` 枚举**（13 个状态）对账 ⇒ 第一次跑就查出 3 处真缺口：`GUIDE`（帮助页，此前**连钩子都没有**，只能按坐标点大厅按钮）、`ARMORY`（报告一直显示却没断言过）、`LEVELUP_MODAL`（只靠"轮询到有卡"就算过） | 第 131 轮 `v1.211g` | 本次 push 触发重拍。**验收**：出现 `_shots/v1210/10b-guide.png` 与 `11b-revive.png`；两行的「实测状态」必须是 `GUIDE` / `REVIVE_MODAL`（写错 ⇒ `::error::` + sweep 退 2）；`11b-revive` 备注列写明**死因行是模拟触发**。⚠ 覆盖检查现报 flow **13 个状态断言过 12 个**（BOOT 白名单=瞬时态）；对照 10/10 |
| 2026-09-25 05:0x | **11b-revive 之前拍的其实不是复活盘**（第 136 轮看图才发现）：死亡后有一段**定格窗** `GAME.deathCineT`（`rawDt` 推进），**窗内弹窗组是隐藏的**（母版 L37741 注释「定格窗结束: 按当前态恢复弹窗组可见」），而 `GAME.state` **已经**是 `REVIVE_MODAL` ⇒ 状态断言成立、图里却只有那张 300×112 的「倒下了」定格小卡 + 冻结战场。判据改成 `MENGSHOU_DEBUG.exclusive()` 的**面板真实可见性**（`revive===true && cine<=0`），**不改游戏代码** | 第 136 轮 `d3937f8` | 本次 push 触发重拍。**验收**：出现**真正的复活盘**（「倒下了!」40px + 「倒下可再战 · 不限次数」22px + 按钮），而不是定格小卡；注解应打印 `11b-revive 采样 state=REVIVE_MODAL 定格窗剩余 cine=0 面板可见=true`。⚠ 同图里那条**粉色斜条是有意设计**（L21342「日冕宽橙 / 瞬斩窄粉」的激光光条），**不是渲染 bug** |
| 2026-09-24 20:06 | **v1.211h 结算页「挑战失败」有 85px 死白** —— 三格没画，就不该占三格的高度。**判据是量出来的、不是看出来的**：裁 `60,560,600x170` → `_qc/_imgstat --inkrows` 得**最长连续空条 255/600 @3× = 85 逻辑px**（该带 43% 的高度上什么都没有）；而 `CONFIG.resultBuildGap` 的注释本就写着「房回礼到本局构筑的缝 = 12」⇒ **作者要 12px、实到 ~85px** ⇒ 是"预留了没画的东西"，不是有意留白（这一条很关键：若是留白就属"有意设计"、不许当缺陷改）。根因在**布局**不在绘制：`bindResultBuildChips` 无条件按 `resultRoomH`(108) 预留，而 `drawResultPanel` 只在 `runSummary.roomClears` 非 0 时才画那三格 | v1.211h `1472422` | **已落地并实测**（本批 `d1302ca`，20:09 UTC）。**同口径**（裁 `60,560,600x170` 原生 + `--inkrows 170` ⇒ 1 桶 = 1 逻辑px）：`15-result-lose` 与 `14-result-win` 的该条带最长连续空条都从 **85/170 → 20/170**（50% → 12%），**两屏同幅收窄 65px**，与预测的 68px 吻合；14-result-win 字节 324838→343833（真的重拍了）。⚠ **我原先把验收写成「255/600 → ≈30/600 以内」，单位是错的**：`--inkrows N` 给的是 **N 个桶**，桶 ≠ 行，只有 N = 图高时 1 桶才等于 1 行；同一条带用 600 桶量出来的 255 是 **43%**，不是 255px。⇒ **以后验收口径固定为「原生裁剪（scale=1）+ N = 图高」**，避免拿两把尺子比。⚠ **边界（为什么这改动不可能压到动作钮）**：`budgetBottom = y + avail = hookY - 8 - unlitH`，**与 y 无关** ⇒ v1.207b 的「不许溢出到动作钮」原样成立，让出的 65px 只进本局构筑的可用预算。⚠ 字母标签原写 **v1.211e 与第 129 轮撞车**（我只在母版里 grep 了字母空间，漏了本文件同样登记字母的三行 —— 量了一处、下了全局结论）⇒ 母版内已改 **v1.211h**，但**提交信息 `1472422` 里仍是 e**，以本行为准 |
| 2026-09-24 20:20 | **v1.211i 强化页路径卡：统计行被价格钮切掉半个字**。症状是 8× 放大才看得见的：「力」被钮的左边框**劈开**，其后的「+3」整段看不见（玩家读到「…· 战力」，而"战力+几"才是这行重点）。**先查历史裁决再动手**：L30876-30877【R22/R32】明写钮是「原位**盖住**节点名/描述尾部」，x 195→238 是**量过** 18px **名字**的（名尾 492>455）⇒ **钮压右侧是有意设计，不动钮**；R32 漏的是这行 **14px 统计行**，而「盖住尾部」与「把一个字劈成两半」不是一回事（同族：v1.211d 章题条压住 16px 字上沿）。修法：这行把同一个属性名写了两遍（「基伤 +0%→基伤 +3%」）⇒ 去掉第二遍省约 32px，而缺口只需约 20px ⇒ **信息不减、字号不动、钮位不动** | v1.211i `69a7a14` | 本次 push 触发重拍（⚠ 只动 `game/`+`dist/` 的 push **不会**触发 art-shots —— 本轮实测：`69a7a14` 触发了 **10** 个 run 而 art-shots **不在其中**，与 §7.3 记的触发条件一致）。**验收（可量的）**：裁 `175,804,100,30` @8× ⇒ 统计行尾部**不得再被钮的边框切开**，且钮左侧应留出空隙（预期该行由约 190px 缩到约 158px，可用约 170px）。**已落地并实测**（本批 `ad655b4`，20:23 UTC）：同一裁窗内现在读到**完整的「· 战力+8」** —— 「力」不再被劈开、原本看不见的 `+8` 露出来了，且文字末端与钮左边框之间留出约 **9px** 空隙（末列 ≈228.75 逻辑px vs 钮左边框 238）。⚠ 该批由**打过收口补丁的** art-shots 产出并正常 commit ⇒ 收口步第二次通过（绿灯现在真的携带信息） |
| 2026-09-24 20:26 | **v1.211j 图鉴·跟宠页白留 96px** —— 与 v1.211h 同型：**给「可能不画的东西」无条件留位**。两处分别查清：① 页脚之上的 msg/knife 槽位（cb+40/cb+64）**只有敌人页会画** —— `drawCodexEnemyDetail` 开头就是 `if (codexTabId() !== 'enemy') return;`（L33495）；② 页脚上方的「花费收益双卡」只在刚解锁跟宠后才画（L33487 的 `hallBeastLast.spent` 分支）。两者都不画时，pet 页在内容底与页脚之间白留 **96 逻辑px**。实测（裁 `44,960,632,160` 原生 + `--inkrows 160`）：最长连续空条 **95/160 = 59%**；**同一几何**裁在卡片区（`44,300,632,160`）只有 **5/160** ⇒ 探针没坏、那块真的是空的。修法：没人画时页脚收到 **cb+42**（取梯子自己的数：双卡上沿 = footY−62）、返回钮 cb+154→cb+92 ⇒ 面板矮 62px，朝 v1.167 写的「底板跟随内容底」走。⚠ 只对 **pet** 收紧（enemy/item 必须留槽；fork 页没核过、**不动**） | v1.211j | 本次 push 触发重拍。**验收（可量的）**：同口径裁 `44,960,632,160` ⇒ 最长连续空条应从 **95/160 掉到 ≈35/160 以内**（预期 ≈34 = 42−8）；`06-beast` 面板高应从 1230 降到 ≈1168（顶部留白相应变大）。**反向核对**：同一裁窗若没变，说明 `codexFitH` 被 viewH 钳住了（那条路不通，得改面板几何——但 v1.185 明令不碰面板几何）。**已落地并实测**（本批 `c3e5c67`，20:29 UTC）：同口径裁 `44,960,632,160` + `--inkrows 160` ⇒ 最长连续空条 **95 → 33 逻辑px** ✓（判据预测 ≈34 = 42−8，实测 **33，差 1px**）；面板随之矮 62px。⚠ 口径说明：面板变矮后 `py` 由 25 变 ≈56，面板内元素整体下移约 31px ⇒ 这个**固定屏幕裁窗**在改前/改后覆盖的面板相对位置并不相同；但两次窗内都完整包含「内容底→页脚」那段空白，所以报出的数**就是那段空白本身的高度**，可比。⚠ 另：图鉴 fork / item 两个页签**从来没被拍过**（19 屏里只有 pet/enemy）⇒ 本轮只对 pet 收紧、fork 明确不动，已立 **O31**（含闭环路径） |
| 2026-09-24 20:36 | **闭 O31：补拍图鉴 fork / item 两个页签**（19 屏 → **21** 屏）—— 此前 19 屏里只有 `06-beast`(=pet) 与 `07-codex-enemy`(=enemy)，所以第 136 轮修图鉴白留时我**判不了 fork 页有没有同型问题**，只能"宁可少改"。**这轮查证发现原计划是错的**：O31 里我写的闭环第一步（"母版 `state()` 加 `codexTab` 字段"）**多余** —— `state()` 早就有这个读数（`codex: tab`，L37930）。实际是**纯 `ci/` 改动**：`codexTab(id)` 是现成钩子（L38579）；新屏 `07b-codex-fork` / `07c-codex-item`；**顺带把 07 的弱判据换掉** —— 它原来只断言 `beast===true`，而 pet/fork/item 三个页签**同样满足** ⇒ 页签切失败也会过（与第 129 轮 `vault===true` **完全同型**），现在三屏都比 `state().codex` 的值 | O31 `a9cbcd7` | 本次 push 触发重拍。**验收（可量的）**：出现 `07b-codex-fork.png` / `07c-codex-item.png`；`index.json` 的 count = **21**；报告里两行的「实测状态」是 `BEAST` 且 `VALUES` 断言通过（切页签失败 ⇒ `::error::` + sweep 退 2，**会红不会静默**）；art-shots 收口步的屏数下限已同步 19→21。⚠ **顺带发现收口自己有个洞**：7d 推送步带 `continue-on-error` ⇒ "算得好、但没推上去"**照样绿**（`a9cbcd7` 那次正是如此：跑完、不在"未完成"里、无失败 workflow，**却没有任何新提交**）⇒ 已补 **7e 收口**：只问"本地 HEAD 在不在 origin/main 上"，不看任何退出码 |
| 2026-09-24 20:40 | **v1.211k：把 v1.211j 的收紧推广到 fork / item 两页**。上一轮只对 pet 收紧，理由是"fork 没截图、判不了"——那就是 O31。现在两屏拍到了（本批 `049ee74`，**21 屏** ✓，`07b/07c` 的 `VALUES` 断言都过），于是**当初缺的那个答案有了**：同口径（裁 632 宽 + `--inkrows 220`）量出 fork **69** / item **91** 逻辑px 的连续空条（pet 收紧前 **95**、收紧后 **33**）⇒ **同型**。判据改成照**画字的守卫**写而不是按页签名硬编码：只有敌人页往 cb+40/cb+64 写字（`drawCodexEnemyDetail` 开头 `if (codexTabId() !== 'enemy') return;`，L33507；L33523 同型），双卡只在 pet 且刚解锁过跟宠时才画 ⇒ `codexTight = tab !== 'enemy' && !duo` | v1.211k | 本次 push 触发重拍。**验收（可量的）**：`07b-codex-fork` / `07c-codex-item` 同口径裁片的最长连续空条应各自掉到 **≈35 以内**；**`07-codex-enemy` 必须不变**（它要留着那两行字，是唯一不能收紧的页）。**已落地并实测**（本批 `465e3ea`，20:44 UTC）：fork **69 → 35** ✓、item **91 → 42**（都掉了 49–62px，与"页脚上移 62px"同量级）；⚠ 因为面板跟着变矮、`py` 下移约 31px，**固定屏幕裁窗**在改前/改后对应的面板相对位置并不相同 ⇒ 残余读数混进了别处的空白行，不能当成"缝还剩 42px"（同 v1.211j 那条口径说明）。**关键对照（比"该变的变了"更重要）**：`07-codex-enemy` 用 `_qc/_band-diff.mjs` 逐带比 ⇒ **只有 31 个像素变了（0.00%，921,600 里）**，且全在 `x[19,25] y[902,907]` 这个 7×6 的小点上（面板外的暗化背景里一粒尘/粒子）—— **y160–1280 每一带都是 0 变化** ⇒ **那一页确实一点没动** ✓ |
| 2026-09-24 20:44 | **v1.211l 选章页有一行提示几乎看不见（对比度 1.54:1）** —— 首次审看 `02-chapters` 时注意到「走进光环开战 · 可跳过」发白，3× 放大确认后**按源码量**：该行是 `#c9c0a8` 压在**苔纸**上 ⇒ `_qc/_contrast.mjs` = **1.54:1**（换米纸/淡苔也只有 1.56–1.58），连 3:1 的大字线都不到；而**同一行**的「房回礼 · …」是 **5.03:1** ⇒ 一行之内一个读得清、一个几乎看不见。`#c9c0a8` 全库 8 处，**7 处都是浅字压深底**（配 `dim > 0` / `skDim`，或与 `#fff7e0`/`#c4c8b8` 这类浅字同屏）⇒ **只有这一处是误用**。判据不是我立的：v1.210n 就是按「16px 小字要 4.5:1」把 **2.25:1** 判成缺陷修掉的，1.54 比那次更差。改成调色板里现成的「纸上副文字」色 `CONFIG.colors.cardDesc = #6a6a5a`（实测 **4.66:1**），**其余 7 处一律不动** | v1.211l | 本次 push 触发重拍。**验收（可量的）**：`02-chapters` 里那行提示（`x+w-4` 右对齐、行 y+9，约在屏幕 `(520..676, 825..840)`）实测对比度应 ≥ **4.5:1**（改前 1.54）；同行「房回礼」应不变 |
| 2026-09-24 20:50 | **v1.211m 两处对比度收尾**：① **v1.211l 只做到 4.26:1，差一点点** —— 它的验收标准写的是 ≥4.5，实测渲染图（`_crop-contrast.mjs`）只有 **4.26**：因为我是拿**调色板上假定的底色** `#e8f0d4` 算的，而这一行真正的底色是 **`#dee6ca`**（暗一档）⇒ 同色相压深一档到 `#5f6050`，同底实测 **4.97:1** ✓。② **合规「AI 显式标识」一直是压在地板上、读不出来**：实测 `01-home`（未调暗）文字 `#f4efe2` 对地板 `#ccd2b4` = **1.36:1**，模态屏叠 50% 黑罩后 **1.17:1**。MEMORY 对「字压**随章变化**的场景底」早有裁决 —— **换色无解，只能描墨边或托底** ⇒ 走**托底**：深墨盘 α0.62（浅地板上 `#5a614e` / 中地板上 `#38402c`），奶油字压上去 **5.62:1 / 9.44:1**，且盘子与字被模态同比例调暗 ⇒ **比值不变**，模态屏一样可读。盘子下沿止于 y=1070（其下 1072 起本来就是那块 208px 近不透明深板） | v1.211m | 本次 push 触发重拍。**验收（可量的）**：`02-chapters` 那行提示的对照色应落在 **`#5f6050`** 且实测 ≥4.5:1（预期 ≈4.97）；`01-home` 的 AI 标识处应出现**深盘**（crop 主色由 `#ccd2b4` 变成深墨绿 ≈`#5a614e`）且奶油字对盘 **≥4.5:1**；`08-settings`（模态）同判 |
| 2026-09-24 20:54 | **v1.211n 选章页"节奏行"3.32:1** —— 这处**不是靠眼睛发现的**，是靠本轮新做的 `_qc/_text-contrast-audit.mjs`（全库字色清点）扫出来的：那行 14px 的「潮 1:30 · BOSS 3:00 · 袋×1 · 武器池 6 把」用调色板成功绿 `#2e8b47`，压在选章面板的纸上 ⇒ **3.32:1**（实测底 `#dee6ca`，`_crop-contrast` 裁窗里占 95.3%），而 14px 正文要 4.5。判据不是我立的：**紧挨着的下一行**（`c.cardGold`）就带着作者自己的同型修复注释【v1.167】「文字用深金(对比度 **2.13:1→5.18:1**)」⇒ 同一处版式里被漏掉的一行。改成同色系深两档 `#256b36` ⇒ 同一实测底上 **5.03:1** ✓。⚠ **只改这一处**：`#2e8b47` 全库 33 处，绝大多数是**按钮填充/描边/刻度**（那里它是底不是字），不能一起改 | v1.211n | 本次 push 触发重拍。**验收（可量的）**：`02-chapters` 底部那行节奏行的 ink 应为 `#256b36`（改前 `#2e8b47`）且实测 ≥4.5:1（预期 ≈5.03）；同屏其余行不得变化 |
| 2026-09-24 20:52 | **v1.211m 验收（本批 `d183d10`）****两项都按渲染图复量通过** ✓。① 选章页提示的 ink 已变成 **`#5f6051`**（正是设的 `#5f6050`），对实测底 `#dee6ca` = **4.96:1**（改前 1.38 / 中途 4.26）。② 合规 AI 标识处出现了**深盘**：裁窗主色从"`#ccd2b4` 浅地板"变成 `#575e4b`(14.3%) 与 `#59614e`(9.7%)，奶油字 `#f4efe2` 压盘上 **5.64–5.89:1**（预测 5.62）—— 且奶油字如今占裁窗 **20.7%**（改成不透明后字实了）。窗内残留的 `#ccd2b4 1.36:1` 是**盘子外面**的裸地板，属预期 |
| 2026-09-24 21:00 | **v1.211p 清单清空：全库字色审计从此只剩"阈值假象"** —— 承上一轮的 `_qc/_text-contrast-audit.mjs`，本轮把它报的"必错"逐条回源码核完并修掉最后两处真缺陷：① **暂停页空态占位**（「还没点被动」「协同未成档」）12px `#6a7a50` = **3.61–4.12:1**（连最浅档都过不了 4.5）——而**结算页的同一个元素早就是对的**（L32026 `var ink="#3c4a30"`，作者注释「【v1.189】结算小字深苔, 对比再抬约一档」，同一梯度 **7.35–8.40:1**）⇒ 同元素在姊妹面板落后一代，直接对齐；② **选角卡的「速」标签**同为 `#6a7a50`，而**同卡其它标签**是 `#2c3a22`（9.38–11.97）⇒ 同排落单，对齐同卡色。另把工具从"单一底色"升级为**渐变三档**（`#dee6ca`/`#e8f0d4`/`#eef4dc`）：只有**整段梯度都不过**才算必错，只栽在最深一角的单列为 near-miss（把那 4 个作者早已调过的色分出去） | v1.211p | **结果（工具自证）**：必错名单 8 种 → 3 种 → **2 种**，而这 2 种已确认都是**阈值假象**（16px「›」箭头按非文字 3:1 只差 0.04；20px 粗体 `loreKicker` 属 WCAG 大字、3.82–4.36 稳过 3:1）⇒ **全库字色审计在"正文 4.5:1"口径下已清空**；`#6a7a50` 站点数 2→1→**0**。**验收（可量的）**：重拍后 `13-pause` 的构筑空态行、选角卡的「速」应为 `#3c4a30` / `#2c3a22`；`_qc/_text-contrast-audit.mjs` 的"必错"应保持 2 种且均为上述两项 |
| 2026-09-24 21:12 | **v1.211q 升级弹窗：两行开头重复（口吃）+ 借机抬字号** —— 这一屏的 32px 标题写「升级三选一 · **点卡锁定**」，紧跟着 18px 的提示又写「**点卡锁定** · 再点才生效 · 金边可略过」：同一句话的同一个短语连着出现两次。走 O27 里的**②减字**，但**只删冗余不删信息**（标题已交代手势，提示从那里接着讲）⇒ 省下的宽度用来 **抬字号 18→20px**（1.70→1.89mm，朝 2.5mm 那条线走一步）。两行都没走 `hudFitText`，所以先核过装得下：20px 下最长一条 ≈270–340px < 面板 580 ✓。**同轮还核完两件文案事**：① 指南页 7 条正文逐条对照游戏，**无矛盾**；其中「强化**六部位**」一度可疑（升级页只有 5 行），查证 `L2371/L34226` ⇒ 六部位=**六个装备槽**（武器/护甲/坠/带/爪/靴），全库 20+ 处同义 ⇒ **术语一致，不改**。② 标点体例：全库 **半角 `标签: 值` 51 条** vs 全角 10 条，但全角那 10 条里**3 条是加菜卡的完整句子**（「加菜约5分钟：…，打完回大厅。」连逗号句号都是全角 ⇒ 本来就对）⇒ 真正的分歧只有 **6 处标签片段**（可合成：/可超进化：/可进化：/差1：/短成就：/三档独立：）；这属**风格取舍**（全角：在中文里并不算错），本轮**不动**、只把账记清 | v1.211q | 本次 push 触发重拍。**验收（可量的）**：新 `12-levelup` 里那行提示**不再以「点卡锁定」开头**，且墨带高度对应 **20px**（改前 18px）；同屏标题仍是 32px 不变 |
| 2026-09-24 21:20 | **v1.211r O27① 抬字：大厅「出战」16→18px、「出战地面」13→18px**（1.51/1.23 → **1.70mm**）。**为什么只抬这两处**：`_qc/_type-candidates.mjs` 当时报「可安全抬 9 处」，我按"每一处都回源码读上下文"逐条核，**前两处一核就废**：① 「走近才捡」11px 与**场物名** 12px **共用一块 96×28 的小盘**（名字在盘内上半、提示在下半）⇒ 抬到 18px 会**撑破盘子 + 层级倒挂**（比上面的名字还大）；② 「健康游戏忠告」13px 带着 **v1.202 的实测裁决**（11px+alpha0.42 ⇒ 3.8:1「强制公告实际看不清」⇒ 正文 12px/标题 13px），且正文 56 字 ×12px = **672 已占满 720 宽** ⇒ 没有余量。第三处「出战」才是干净的：胶囊 **72×38**、2 字 18px≈36px，宽高都富余 ✓；「出战地面」在 220×154 的奶油铭牌里、4 字 18px≈72px ✓ | v1.211r | 本次 push 触发重拍。**验收（可量的）**：`01-home` 上「出战」与「出战地面」的墨带高度对应 **18px**（改前 16/13px）；**项目自己的字号门禁必须仍绿** —— 已本地实跑 `ci/text-scale-check.mjs`：**PASS · 台账 15/15 条都有理由、没有失效条目** ✓ |
| 2026-09-24 21:20 | **巡检新增「冷启动耗时」读数（只记录，先不定阈值）** —— 起因：这一条**从来没被测过**，而巡检开头那两段**盲目等待 4000+1800ms** 本身就是"不知道要等多久"的产物。测什么：`navigation` 的 DCL/load/duration + **从 goto 到 `state().name !== 'BOOT'`（能交互）** 的毫秒数，走 `::notice::` 注解 + `run.txt`。为什么值得测：inline 母版 4.5MB（贴图 base64）、dist 1.85MB + 178 素材，**"首次打开多久能玩"是真机上最直接的体验项之一，却一直没人量过**（MEMORY 里记的"真机跑一局 59.9fps"只覆盖帧率，没覆盖启动）。⚠ 本轮**不改那两段等待**（保持批次可比），也**不设阈值**（P8：先 dump 真实数据再定判据）| 第 141 轮 `ci/screen-sweep.mjs` | 本批注解应出现 `冷启动 DCL …ms · load …ms · navigation …ms · 到非 BOOT …ms`。**下一步**（拿到基线后）：若启动明显偏慢就找瓶颈（音频解码 / base64 贴图 / 首帧）；若很快，则回头把那 5.8s 盲等改成条件等待（省 CI 时间） |
| 2026-09-24 21:32 | **v1.211s O27 样板屏（图鉴·跟宠卡）：删掉重复的一行，把空间给字号** —— 本轮不再等答复、直接做**最保守的那一版**样板：**不减条目、不动几何**，只删**真正的重复信息**。查证：卡内第 4 行是 13px 的 `petUnlockStep(d)`，内容是「进度解锁 · **差N金**」；而**同一张卡右侧的签**（L33716）在金币不足时画的正是「**差N金**」，**页面头部**（L33467 `obtain`）又画一次 ⇒ **同一信息一屏出现三次** ⇒ 删卡内那份（信息仍在，两处），每张卡省一整行，腾出的空间把上一行 **14→16px**（1.32→1.51mm）。⚠ 只抬这一行：上面那行 `desc + clash` **没走 `hudFitText`** 且 clash 可能很长，先抬有横向溢出风险（下一轮先补宽度兜底）。**刻意不动 `codexPetRowH=118`** ⇒ 列表长度与 `codexContentBottom()` 全不变，**零布局风险** | v1.211s | 本次 push 触发重拍。**验收（可量的）**：`06-beast` 每张卡**只剩 3 行字**（原 4 行），且「伤N / Ns · 星级」那行墨带高度对应 **16px**（改前 14px）；卡右侧的「差N金」签与页面头部那条**必须仍在**；低于 18px 的站点数应为 **214**（改前 215，删掉的那一行不再计入）✓ 已本地复核 |
| 2026-09-24 21:5x | 🔴 **本轮最重要的一条：一批图「属于哪个构建」不能看提交的父 SHA** —— 我拿 `5dcd707`（截图提交）的**父提交 = `31334bd`** 当证据去验 v1.211s，量出来的却是**改前**的像素（卡片区与上一批**逐位相同**，整图只差 30 个像素、全在 `x[13,18] y[254,259]` 的页签小字形上）。**回查时间线才发现真相**：`_shots/index.json` 的 `at=21:33:40Z`、提交在 `21:33:42Z` ⇒ 那批图是 **run A（`2a66a5b`，改前）** 渲的，它的 7d 先 `git pull --rebase` 再 push，于是**被 rebase 到了更新的 `31334bd` 上**；而 run B（`31334bd`，21:33:05 起、21:36:08 完）**一个提交都没产生**（远端 main 仍是 `5dcd707`）。**修法（三重）**：① 7b 把 `renderedSha` + 入口文件 `entrySha256_16` 写进 `index.json` 并打印；② **7b′ 加一条出身对账** —— 声明的 `renderedSha` ≠ 检出 HEAD 就报红（7b′ 跑在 7c/7d 之前，此刻 HEAD 正是巡检真正渲的 sha）；③ 提交标题带上 `rendered <sha7>` ⇒ 出身随提交本身走。**判读从此只认 `index.json`**，不看 git 拓扑 | 第 142 轮 | 本次 push 触发重拍。**验收（可量的）**：新批次 `_shots/index.json` 必须自带 `renderedSha`（= 本次 push 的 sha）与 `entrySha256_16`（本机实测 `c9f6dee0725e2f43`），提交标题里出现 `rendered`；`06-beast` 卡内只剩 **3 行**文字（墨带 4→3 条）、第 3 行墨带高度对应 **16px** |
| 2026-09-24 21:5x | 🔴 **顺带查出一件更糟的：`_yaml-check.mjs` 对 CRLF 文件一直是假绿** —— 起因是给 art-shots 补出身校验后我想先验 YAML，它却报「总 step 数: **0** / 问题数: **0**」。查因：JS 里 `.` **不匹配 `\r`**，而 `$`（无 `m` 标志）**只在串尾**匹配 ⇒ `(.*)$` 在 `…建目录\r` 上**永不匹配**；而仓库里 16 个 workflow 基本全是 CRLF ⇒ **我此前所有「YAML 校验通过」都是空的**。修：读入即归一化行尾；加硬判据「有 `steps:` 却解析出 0 个 step = 报错」（不允许「什么都没解析到」再长得像「没问题」）；补 8 条自测（含 **CRLF 回归对照**、有 `steps:` 但认不出、制表符、空 run 块 ×2 写法、缩进、无 `steps:` 不凭空造错）。**修完立刻见效**：16 个 workflow 从「全 0 step」变成 5/5/11/…/40 个 step，问题数 **0/16** ✓ | 第 142 轮 | 本机已跑：`_yaml-check.mjs --selftest` **8/8**；16 个 workflow 结构问题数 **0** |
| 2026-09-24 21:5x | 🆕 **新增量具 `_qc/_sh-parse.mjs`：workflow `run:` 块的 `bash -n` 语法门禁** —— 为什么需要：这些 workflow 靠 `continue-on-error` + `\|\| true` + `exit 0` **永远绿**，run 块里一行 bash 语法错就能把整条回读链**静默**掐断，而云端 Actions 日志**要鉴权、我读不到**（P13）⇒ 落 workflow 前在本机验语法是**唯一不依赖云端**的闸门。两处坑已防：① `${{ ... }}` 是 GitHub 表达式不是 bash，先替换成占位符（否则全是假报错）；② 必须做**阴性对照**（自测里故意塞坏语法，确认真报错）。本机 bash = `D:\Git\bin\bash.exe` 5.2.37。**实测**：16 个 workflow 共 **61 个 run 块**，语法错 **0**（含本次改过的 art-shots 7 块） | 第 142 轮 | 自测 **6/6**；`node _qc/_sh-parse.mjs <所有 workflow>` = 61 块 / 0 错 |
| 2026-09-24 21:50 | ✅ **第 142 轮验收（本批 `81b0031` · rendered `62c2189`）：v1.211s 首次在云端真实渲染里验过** —— 上一批是**改前**的图（见上一行），本批才是。`_shots/index.json` 自证 `renderedSha=62c2189a…`、`entry=萌兽消消岛.html`、`entrySha256_16=d90bc5f1c2e4ccd8`、`count=21`，提交标题也是 `rendered 62c2189` ⇒ **三重出身对得上**。**验收（可量的）**：卡0（裁 `162,186,420x130` @3×）inkrows 从 **4 条墨带**（23-43/57-70/78-91/103-115）变成 **3 条**（23-43/57-70/78-91），第 4 带整段归零、最长连续空条 22→**36**（108 逻辑px）；第 3 带出现 87.8/85.2/87.1 的粗笔画 = 字号 14→16px 的痕迹；整图 band-diff vs 改前批次 = **30632 px / 3.32%**（而「改前 vs 更早」只差 30 px ⇒ 这次真的换构建了）。肉眼复核（3× 裁片 + 整屏）：7 张卡**全部只剩 3 行**、统计行明显变大、**卡右「差N金」签与页面头部 `obtain` 行都在** ✓ | 第 142 轮 | — |
| 2026-09-24 21:52 | ⚠ **上一行我写错了一个口径，就地更正**：我把 `entrySha256_16` 的期望值写成本地 `c9f6dee0725e2f43` —— 那是**工作区 CRLF 版**的 sha256，而 7b 算的是 **git blob（LF）** 的 sha256。同一对象、两种行尾 ⇒ 实测 `d90bc5f1c2e4ccd8`。已核：`git show HEAD:game/萌兽消消岛.html` 的 sha256 前 16 位**正是** `d90bc5f1c2e4ccd8`，而工作区那份去掉 `\r` 后也得到同一个值（4724574→4685241 字节、39333 处 CRLF）。**读法**：要跟 `entrySha256_16` 对账，就用 `git show HEAD:<file>`（或把工作区转 LF），**别直接哈希工作区文件**。（顺带一条：本机 `core.autocrlf=true` ⇒ 入库 blob 全是 LF，只有工作区是 CRLF，所以云端 runner 上跑的一直是 LF 版） | 第 142 轮 | — |
| 2026-09-24 22:0x | **v1.211t 图鉴·跟宠卡：补齐上一轮欠的两件事（宽度兜底 + 抬字），并把被我改歪的竖向平衡拨回来** —— 上一轮删掉第 4 行时**刻意不动几何**，代价是墨迹从 11..80（块中心 45.5）掉到卡片中心 59 之下 **13.5px**：上留白 **11** / 下留白 **38**，看着像「删漏了一行」而不是有意排的。本轮同批做三件事：① 给 `desc + clash` 补上 `hudFitText(…, 396)`（**先兜底再抬字**，这是上一轮留的前置条件）；② desc **15→17px**（1.42→1.60mm；最坏组合 ≈365px < 396px，兜底是安全网、正常档位不触发省略号）；③ 三行 y 整体 **+13**（22/52/72 → **35/65/85**）⇒ 墨迹 24..93.5（中心 58.75），与图盘(8..110)、右签(36..80，中心 58)三者中心对齐。**几何/行高/条目数一律未碰** | 第 143 轮 | 本次 push 触发重拍。**验收（可量的，预测先行）**：卡0（同一裁切 `162,186,420x130` @3×）三条墨带应整体**下移 ≈13 桶**（第 1 条 23-43 → **≈36-56**，第 3 条 78-91 → **≈91-104**）；desc 带应变高（15→17px）；**顶部空条 22 → ≈35**、**底部空条 36 → ≈25**（总留白不变，变的是上下分配）；卡右「差N金」签与头部 `obtain` 行必须仍在；整图 band-diff vs 上一批的变化应**集中在卡片文字列**（x≈118..514），不是全屏 |
| — | ✅ **两条「我自己会不会给假保证」的线，第 143 轮实测干净，别再重跑**：① `_qc/_gate-vacuity.mjs` 空目录实跑 53 个 `ci/*.mjs` ⇒ **HONEST 34 · 真空转 0** · 读到真文件（探针口径所限）5 · 待看 3 · UNKNOWN 0；同批报的「真仓库里就红 11」**全部解释得通、不是缺陷** —— 逐个查依赖：**10 个 import playwright**（本机没有），第 11 个 `sfx-freq-vs-loud.mjs` 缺的是**浏览器产出的输入**且它自己打印「先跑 ci/sfx-runtime-freq.mjs」再 exit 3（**诚实**，且 `sfx-runtime-freq.yml` L35/L37 确实调它 + 有独立 `.selftest.mjs`）。② 调用覆盖扫描（`_qc/_scan-ci-gate-cov.mjs`）：62 个 `ci/*.mjs` 里**带 `--selftest` 却没有任何 workflow 调用的「死门禁」= 0 个**；无人调用且无自测仅 3 个（`find-dead-art` / `resample-audio-44100` / `trim-bgm-seam`，都是工具不是门禁）。⚠ 这两条都是**先验证再下结论**才没变成误报（P1）。 | — | — |
| 2026-09-24 22:06 | ✅ **v1.211t 验收（本批 `91c3b90` · rendered `7da8e8c`）：三条预测全部命中，一处模型偏差如实记** —— 出身链条这次最强：`index.json.renderedSha = 7da8e8c…`（= 我的推送 sha）、`entrySha256_16 = 6221a55e40f4f5ec` **且等于 `git show HEAD:game/萌兽消消岛.html` 的 sha256 前 16 位** ⇒ 能确认截图是照**哪一份字节**渲的。**实测（卡0，同一裁切 `162,186,420x130` @3×）**：三条墨带各下移 **+13 / +12 / +14**（23→36、57→69、76→90），顶部空条 **23→36**（预测 ≈35 ✓）、底部空条 **39→27**（预测 ≈25 ✓）、带数仍 **3**；3× 放大肉眼：整块下移成立、**desc 与 stats 现在同样大小**（15→17px 生效）、右签仍在。**⚠ 一处预测偏了**：我按 em 盒估墨迹（22px 行按 ±11px）预测「上 24 / 下 24.5」，实测 **上 30 / 下 22**（相对卡片 118 高：卡顶 bucket 6、卡底 124）。原因：CJK 墨迹只占约 **0.75em 且偏上**（实测 22px 行的墨带只有 **20** 桶 ≈ 20px，不是我估的 22），而 `textBaseline=middle` 居中的是 **em 盒不是墨盒**。**⇒ 判据：算文字竖向位置要用「实测墨带高」，别用字号推** —— 这条已写进母版注释。**另**：同批 `_imgstat` 的「带内墨量」当字号判据**不干净** —— 同字号的两条带也掉了（×0.917 / ×0.745），因为下移后压在**深浅不同的渐变底**上，「偏离行中位数 >14」的像素数跟着漂 ⇒ **墨量只能当参考，字号结论以放大图为准** | 第 143 轮 | — |
| 2026-09-24 22:14 | 🔴 **本批 `d87c394` 是红的，死因来自回读通道（这就是第 142 轮补它的价值）**：`run.txt` 里躺着 `::error::11-battle 目标屏断言不通过：实测 state=LEVELUP_MODAL / 期望 PLAYING` + `=== exit=2 ===`。**先判归属再动手**（P1）：这批渲的是 `16446e0`，与上一批**通过**的 `7da8e8c` **游戏代码逐字节等价**（只多 5 行注释）⇒ **不是产品改动引起的**。**根因**：`11-battle` 的断言读的是「清场 + 强回 PLAYING 之后 **400ms** 的 state」，而**战斗中 XP 还在涨**（母版 L22332 `run.pendingLevels++`）⇒ 母版 L4332/L6838 会**合法地**再次 `openLevelUp()` ⇒ 那一读时常是 LEVELUP_MODAL；可磁盘上那张图**确实是**「轮询里 PLAYING 且有怪」时拍的帧 ⇒ **断言量与结论说的不是同一个对象**（P1，而且长在我自己的探针里）。**修法**：加 `bShotPlaying`（那一帧的**出身**），判据改成「产物是不是 PLAYING 且有怪时拍的」；只有走兜底补拍（`bBestShot < 1`）那条路时没有出身、才仍按当前 state 判。**注意退出码同源**：`exit=2` 来自 L713 的 `dupList`（`stateOk === false` 进列）⇒ 只改注解不够，`stateOk` 的计算必须一起改。**控制（`_qc/_scan-battle-verdict.mjs`，5/5）**：从文件里**抽出那一行真表达式**求值 —— ②拍到真帧+报告时弹窗 ⇒ 新 true / **旧 false**（证明修的是真问题）、③只有补拍帧+当前非 PLAYING ⇒ **仍红**（该红的还红） | 第 143 轮 | 本次 push 触发重拍。**验收（须能区分）**：新批次 `run.txt` 里必须出现 `::notice::11-battle 帧出身 bShotPlaying=…`（证明新代码真跑了），且**没有** `::error::`；⚠ **光看「这批绿了」不算证据** —— 这个 flake 是随机的，可能本来就不复发；**notice 行在不在**才是判据；`ci/out/screens.json` 的 `11-battle` 应多出 `shotPlaying` 字段 |
| — | 🔧 **要查外部资料时怎么走（第 141 轮实测，省得每轮重试）**：① `web_search` 背后的 modsearch 桥**会整体挂**（firecrawl 匿名 403：`your IP address looks suspicious`）—— 挂了就换路，别重试；② **Bing 对本会话返回"没有与此相关的结果"**（连正常查询也是 ⇒ 是机器人门，不是真没结果），`browser_evaluate` 取 `#b_results` 也是空的；③ ✅ **可用**：`web_fetch` 直接打 **`https://lite.duckduckgo.com/lite/?q=<urlencoded>`** —— 纯 HTML、无需 key、结果带摘要，本轮就是靠它拿到中文可读字号的三个独立来源。④ Apple HIG 那种 SPA 页 `web_fetch` 只回标题（要渲染），`browser_navigate` 用 `networkidle` 会 30s 超时（试 `domcontentloaded`） | — | — |
| — | ⚠ **时间列口径（第 136 轮实测发现）**：表头写「时间(UTC)」，但 **L43–L49 那几行用的是本地时间（UTC+8）**，比 UTC 晚 8 小时 —— 判据：它们写的是 `2026-09-25 01:5x…05:0x`，而当时**实测 UTC 才 `2026-09-24 20:06`**，未来时间不可能是 UTC。上面这条新行起按**实测 UTC** 写。 | — | — |
| — | ⚠ **①b 的已知局限（本轮实测）**：它比的是"截图的 git 时间 vs 母版时间"，所以**只改注释/代码、不动像素的提交也会被它标成 STALE** —— 那种情况下图**本来就该不变**。判读顺序：先看本表（这批图对应哪一版、差哪几处改动），再看 ①b 的时间差；**时间差单独不足以判定"图过时"**。 | — | — |
| — | ⚠ 教训（第 114 轮实测两次）：① `art-shots` **只认 `_shots/**` 的改动**（改了画面就必须动一次本目录，否则那一轮不会重拍）；② 云端重拍会**和我同时往 main 推** ⇒ 两边分叉、`pull --ff-only` 会被拒，正确姿势是 `git pull --rebase origin main` 再推。 | — | — |

> 为什么记这一栏：①b 巡检只会说"截图落后几小时"，**说不出"这批画对应哪一版、差的是哪几处改动"**。
> 有了这张表，"能不能拿这批图下结论"就不用靠猜 —— 与 `MEMORY.md` 里"环境事实必须带复核日期"同一条道理。

