# 全界面巡检截图（art-shots 流水线自动落盘）

推这个目录下任何改动即触发；产物同时写 ci/out 与 _shots/v1210/。

## ⚠ 新鲜度规矩（2026-09-24 实测踩到，写在最前面）

**截图不会随代码自动更新。** 触发条件只有 `_shots/**` 与 workflow 文件本身
（`.github/workflows/art-shots.yml` 的 `on.push.paths`）——**改了游戏再 push 不会刷新它们**。
实测：v1.210m/n 四个提交之后，`v1210/` 里仍是那四个提交**之前**的画面，
而当时的巡检照样打印「产物三形态同源 ✓」——**版本串同源 ≠ 你看到的画是同源**。

⇒ 判新鲜度只看一件事：**`_shots/v1210/*.png` 的时间 vs 母版最后一次改动的时间**。
`_qc/_audit-omissions.mjs` 已把这条加进巡检（截图落后于母版即报 STALE）。

## 怎么刷新

1. push 任意改动到本目录（这份 README 也算）→ 云端 `art-shots` 跑一遍，把新图 + `run.txt`
   **提交回仓库**；也可走 `workflow_dispatch` 手动触发。
2. `ci/out/` 被 `.gitignore` 忽略 ⇒ workflow 里必须 `git add -f`，否则提交阶段「绿着但啥也没提」（已修）。
3. 新图落地后本地重建看板：`node _qc/_shots-gallery.mjs`。

## ⚠ 派生视图**天然落后一批**（设计如此，不是故障）

`v1210-phone/`（`_qc/_phone-view.mjs` 从 `v1210/` 缩出来的真机视图）与 `gallery.html` 都是**派生件**：
它们的更新本身也算一次 `_shots/**` push ⇒ 又会触发下一轮重拍。所以派生件永远比最新一批**早一批**，
这是循环，不是缺陷。**判断"能不能拿它下结论"看的是：这一批与母版之间有没有改到它承载的东西** ——
真机视图承载的是**文字/尺寸**，若那一轮只改了地面贴图，它就仍然有效。

## 刷新记录（每次重拍写一行：这批画到底对应哪一版）

| 时间(UTC) | 触发原因 | 对应母版 | 结果 |
|---|---|---|---|
| 2026-09-24 13:02 | v1.210 系列收尾 | v1.210s | 13 张 720×1280 ✓ |
| 2026-09-24 14:40 | **v1.210t 装备袋改程序化剪影**（战场上的袋子不再是木箱）+ **v1.210u 帮助页字号 17/18/19→21px** —— 这两改**动了画面/文字**，13:02 那批已不代表当前版本 | v1.210u `cca8e03` | **已落地 `400f26e`（14:37）**：13 张，其中 10 张有变化；派生件（`v1210-phone/` + `gallery.html`）随后本地重建 ✓ |
| 2026-09-24 15:00 | **v1.210v 设置页字阶**：操作提示 17→18px、「音频三档」小签 14→18px（**改了文字 ⇒ 08-settings 必须重出**） | v1.210v `dafe589` | **已落地 `ac6feaa`（15:03）**：08-settings 已重出；真机直看 18px 未裁切 ✓ |
| 2026-09-24 15:20 | **v1.210w 加菜面板两句提示 15/14→18px**（`_qc/_type-candidates.mjs` 筛出的可抬站点里先做掉的 2 个） | v1.210w `033c7cc` | **已落地 `1874f92`（15:26）**：10-daily 已重出；1:1 直看两句 18px **未裁切、未压返回钮** ✓ |
| 2026-09-24 15:40 | **v1.210x 承诺守卫 + 承诺句前提注释**（只改注释与 dist/deploy，**不动任何像素**） | v1.210x | 重拍后应与本批图逐字节相同 ⇒ art-shots 可能报"无产物变化"（**这是对的，不是失败**） |
| 2026-09-25 01:20 | **v1.211a 大厅主题签状态字 11→15px**（运行时实测挑出：它是"必须动手"的短词里最小的，1.04mm） | v1.211a | 本次 push 触发重拍（**01-home 会变**） |
| 2026-09-24 17:47 | **v1.211b 大厅地面草簇"看得见"**（`paintHallGround` 草簇 s 14..26→**50..62** 画布 px、边距 48→88）—— 真机簇高 **0.70..1.30mm → 2.56..3.08mm**，是**尺度账**不是口味：旧值连项目自己的 2.5mm 可读线都没进过（同族：字号那条线）。判据来自新量具 `_qc/_imgstat.mjs`（阴性对照 11/11）实测大厅 y800-960 段 cf **18.9** / edgePct **4.00%**，而同一屏自己的 CTA 段 cf **84.7**。**动了画面 ⇒ 01-home 必须重出** | v1.211b | **已落地 `a6e7f37`（17:49）**：`01-home` 467719→469439 字节。`_qc/_band-diff.mjs` 实测目标段 **3160 px 变化（2.74%、平均Δ86.7）**，y0-160/y1120-1280 **0 变化**。⚠ 但 cf 18.9→19.3、edgePct 4.00%→4.42% ⇒ **尺寸修好了、密度远远不够**（7 簇/瓦 ⇒ 整块地板仅 ≈15 簇） |
| 2026-09-24 17:52 | **v1.211c 草簇密度 7→16 簇/瓦**（同一轮内闭环）：尺寸已进可读区间，只补数量 ⇒ 整屏 ≈34 簇 | v1.211c `35f710b` | **已落地 `de56022`（17:54）**：01-home 469439→482918 字节。判据（`_qc/_imgstat.mjs --bands 8`，对**同一 `--bands` 口径**比三批）：目标段 y800-960 的 **edgePct 4.00% → 4.42% → 7.73%**、edge 1.15→1.25→**2.09**、cf 18.9→19.3→20.7、uniq 749→760→788、近灰 12.8%→10.1%；y960-1120 edgePct 9.69→9.72→**9.83**。**无副作用**：y0-160 与 y1120-1280 三批全等（14.30% / 16.25%），CTA 段 y480-640 **20.83→21.00（没被拉低）**、cf 84.7（保住）。⚠ cf 不作局部判据（整幅离散度，对局部改动不敏感）|
| 2026-09-24 18:12 | **11-battle 拍法改了（`ci/screen-sweep.mjs`，第 126 轮）**：原来钉死在"进战斗后第 7 秒"—— 那个秒数是为了给 12-levelup 攒一个升级弹窗，**不是为拍战斗** ⇒ 拍到的是空场。现改为轮询到真的有怪（`enemyCount ≥ 10`，上限 60s）才拍。依据：`_qc/_imgstat.mjs` 量出 11-battle 的 edgePct **4.37% 是 13 屏最低**，而它是玩家花 90% 时间的一屏；且它连「独特色数/主色占比」都是 `-`（那段量只写在 STEPS 循环里）| 第 126 轮 `de91ab9` | **已落地 `3ff8613`（18:05）**。云端注解实证：`11-battle 采样密度 同屏怪=10 峰值=10 目标=10 轮询=63 结束=达到 10 只`；`sweep` 结论 **success**（12/13 两屏没被带崩）。报告里 11-battle 首次有读数：**独特色数 724 · 主色占比 21.9%**，备注列「同屏怪 **10** 只」；12-levelup 697/32.9%、13-pause 709/32.4% 不再是 `-`。`_imgstat` 量：**edgePct 4.37%→6.55%、edge 1.33→1.96、cf 61.7→69.3、lumSd 35.1→40.0**；`01-home` 逐项不变（本轮未动游戏代码）|
| 2026-09-24 18:22 | **11-battle 取"本次最密帧"（`ci/screen-sweep.mjs`）**：上一版在**没命中阈值时**照样按"循环结束那一刻"拍 ⇒ 云端实测拍到过 **0 只怪**的帧（比我原本要修的"第 7 秒空场"还空），且密度标签记的是瞬时值、**描述的不是这张图**。现改为怪数刷新新高就立刻覆盖一张，标签改报**这张图自己的**怪数 | 第 126 轮 `cd4aee0` | 本次 push 触发重拍。**三批实测（`_imgstat`）**：原第 7 秒固定帧 edgePct **4.37%** → 命中阈值那次 **6.55%** → 超时拍到 0 只那次 **4.08%（比我修的问题更差）**。验收：本批注解应出现「这张图同屏怪≥1」，且 edgePct 不再低于 4.37% |
| 2026-09-25 01:5x | **把"地面重复"这件事量成两个频段（`_qc/_tile-repeat.mjs` 加带通，对照 11/11）** —— 起因是我在新 11-battle 图上看见"一圈圈按网格重复的浅环"，而母版 v1.210g/h/m 已为同一现象调过三轮（**属有意设计，不许凭观感判死刑**）| 第 127 轮（只动 `_qc/` 与本文件，**未动游戏代码**）| **实测（带通，低频=σ12 模糊；高频=原图−低频）**：战场地面 y300-1150 **低频 3.66 vs 高频 1.95 ⇒ 低频占主导**，与肉眼看见的"环"一致；大厅地板 y900-1060 v1.211a **2.56/0.48（5.3×，典型壁纸）→** v1.211c **4.31/3.76（1.15×）** ⇒ v1.211b/c 那笔加草**把低频占比从 84% 压到 53%**（加的是质感不是壁纸）。⇒ **结论：两处都不要改**。⚠ 以后凡论"重复"必须分频段说：`amp` 混报会把"加草"读成"加壁纸" |
| 2026-09-25 02:0x | **补两屏"每个玩家每局都会看到、却从来没被拍过"的：结算（胜/负）** ⇒ 巡检 13 屏 → **15 屏**。发现方式不是"看着不对"，而是**数一数哪些屏根本没被拍过**：13 屏里没有结算页，而 `win()`/`lose()`/`resultBuild()` 三个钩子母版（L39150/L39155/L39015）早就给了却没人用 | 第 128 轮（`ci/screen-sweep.mjs`，**未动游戏代码**）| 本次 push 触发重拍。**验收**：`_shots/v1210/` 应出现 **14-result-win.png / 15-result-lose.png**；报告里这两行的「实测状态」必须是 `RESULT_WIN`/`RESULT_LOSE`（写错会进 dupList ⇒ sweep 退 2，**会红不会静默**），备注列出现「结算卡带 N 条 · 压住=否」。⚠ `chips` 字段名我第一版按印象写错（母版实际是 **`chipN`**），会静默变 null —— 已按源码改正 |
| 2026-09-25 02:2x | **v1.211d 修结算页头行被削平**（`drawResultPanel` 画序错：章题条画在文字**之后**，把 16px 字上沿切掉 6px = 字身 37.5%）。**这一屏本轮才第一次被拍（13→15 屏），第一次看就看见了** | v1.211d | 本次 push 触发重拍。**验收**：`14-result-win` / `15-result-lose` 的**头行字顶不得再有横向切口**（`node _qc/_crop.mjs <图> 200 22 340 70 4` 放大复核）；条子占 py+2..py+24、16px 字上沿 py+24 ⇒ **重叠 0px**。⚠ 顺带证实：**"把没拍过的屏补进巡检" + "能放大看细节"** 这两件事是发现它的直接原因 |
| 2026-09-25 03:0x | **补拍仓库「天赋」页签**（`05b-vault-talent`）—— 由新检查 `_qc/_shot-coverage.mjs` 查出：05-vault 拍的是默认 craft 页，**talent 页从来没有截图**。判据不是 `vault===true`（craft 页同样为真 = 空门），而是给 `state()` 新增的 `vaultTab` 字段做**值比对**（巡检新增 `VALUES` 表） | 第 129 轮 `v1.211e` | 本次 push 触发重拍。**验收**：出现 `_shots/v1210/05b-vault-talent.png`；报告该行「实测状态」应为 `HOME` 且断言通过（`vaultTab=talent`）；注解应打印 `05b-vault-talent … vaultTab=talent`。⚠ 同时把 `want` 里的死条目 `openPetCard`（母版没有这个钩子）删掉 |
| 2026-09-25 03:3x | **补拍「进场过场」（`11a-entrance`）** —— 每个玩家每局开头都会看到，此前从没被拍过。发现路径：修完 v1.211d（章题条压住文字上沿）后先查**同族版式**，`drawChapterMapStrip` 全文件只有两处调用，另一处就是这里；逐行读过画序确认那处**没问题**（条子在自己的 `clip()` 里、且在文字之前），但顺势发现**这一屏根本没被拍过** | 第 130 轮 `v1.211f` | 本次 push 触发重拍。**验收**：出现 `_shots/v1210/11a-entrance.png`；注解 `11a-entrance 采样 过场 t=…/… kind=chapter first=…`；报告该行备注列带**采样时刻**。断言 `live===true` 不通过会 `::error::` + sweep 退 2。⚠ **覆盖检查查不到它**（时间是驱动、无钩子可开）⇒ 已给母版补 `enterCine()` 探针并写进 `want`；**"有钩子的屏" ≠ "所有屏"** 这句已写进 `_shot-coverage.mjs` 文件头 |
| 2026-09-25 04:0x | **补拍「复活盘」+「怎么玩帮助页」，并把 4 处缺失的状态断言补齐** —— 判据从"钩子名"换成**游戏自己声明的 `GAME.flow` 枚举**（13 个状态）对账 ⇒ 第一次跑就查出 3 处真缺口：`GUIDE`（帮助页，此前**连钩子都没有**，只能按坐标点大厅按钮）、`ARMORY`（报告一直显示却没断言过）、`LEVELUP_MODAL`（只靠"轮询到有卡"就算过） | 第 131 轮 `v1.211g` | 本次 push 触发重拍。**验收**：出现 `_shots/v1210/10b-guide.png` 与 `11b-revive.png`；两行的「实测状态」必须是 `GUIDE` / `REVIVE_MODAL`（写错 ⇒ `::error::` + sweep 退 2）；`11b-revive` 备注列写明**死因行是模拟触发**。⚠ 覆盖检查现报 flow **13 个状态断言过 12 个**（BOOT 白名单=瞬时态）；对照 10/10 |
| 2026-09-25 05:0x | **11b-revive 之前拍的其实不是复活盘**（第 136 轮看图才发现）：死亡后有一段**定格窗** `GAME.deathCineT`（`rawDt` 推进），**窗内弹窗组是隐藏的**（母版 L37741 注释「定格窗结束: 按当前态恢复弹窗组可见」），而 `GAME.state` **已经**是 `REVIVE_MODAL` ⇒ 状态断言成立、图里却只有那张 300×112 的「倒下了」定格小卡 + 冻结战场。判据改成 `MENGSHOU_DEBUG.exclusive()` 的**面板真实可见性**（`revive===true && cine<=0`），**不改游戏代码** | 第 136 轮 `d3937f8` | 本次 push 触发重拍。**验收**：出现**真正的复活盘**（「倒下了!」40px + 「倒下可再战 · 不限次数」22px + 按钮），而不是定格小卡；注解应打印 `11b-revive 采样 state=REVIVE_MODAL 定格窗剩余 cine=0 面板可见=true`。⚠ 同图里那条**粉色斜条是有意设计**（L21342「日冕宽橙 / 瞬斩窄粉」的激光光条），**不是渲染 bug** |
| 2026-09-24 20:06 | **v1.211h 结算页「挑战失败」有 85px 死白** —— 三格没画，就不该占三格的高度。**判据是量出来的、不是看出来的**：裁 `60,560,600x170` → `_qc/_imgstat --inkrows` 得**最长连续空条 255/600 @3× = 85 逻辑px**（该带 43% 的高度上什么都没有）；而 `CONFIG.resultBuildGap` 的注释本就写着「房回礼到本局构筑的缝 = 12」⇒ **作者要 12px、实到 ~85px** ⇒ 是"预留了没画的东西"，不是有意留白（这一条很关键：若是留白就属"有意设计"、不许当缺陷改）。根因在**布局**不在绘制：`bindResultBuildChips` 无条件按 `resultRoomH`(108) 预留，而 `drawResultPanel` 只在 `runSummary.roomClears` 非 0 时才画那三格 | v1.211h `1472422` | **已落地并实测**（本批 `d1302ca`，20:09 UTC）。**同口径**（裁 `60,560,600x170` 原生 + `--inkrows 170` ⇒ 1 桶 = 1 逻辑px）：`15-result-lose` 与 `14-result-win` 的该条带最长连续空条都从 **85/170 → 20/170**（50% → 12%），**两屏同幅收窄 65px**，与预测的 68px 吻合；14-result-win 字节 324838→343833（真的重拍了）。⚠ **我原先把验收写成「255/600 → ≈30/600 以内」，单位是错的**：`--inkrows N` 给的是 **N 个桶**，桶 ≠ 行，只有 N = 图高时 1 桶才等于 1 行；同一条带用 600 桶量出来的 255 是 **43%**，不是 255px。⇒ **以后验收口径固定为「原生裁剪（scale=1）+ N = 图高」**，避免拿两把尺子比。⚠ **边界（为什么这改动不可能压到动作钮）**：`budgetBottom = y + avail = hookY - 8 - unlitH`，**与 y 无关** ⇒ v1.207b 的「不许溢出到动作钮」原样成立，让出的 65px 只进本局构筑的可用预算。⚠ 字母标签原写 **v1.211e 与第 129 轮撞车**（我只在母版里 grep 了字母空间，漏了本文件同样登记字母的三行 —— 量了一处、下了全局结论）⇒ 母版内已改 **v1.211h**，但**提交信息 `1472422` 里仍是 e**，以本行为准 |
| 2026-09-24 20:20 | **v1.211i 强化页路径卡：统计行被价格钮切掉半个字**。症状是 8× 放大才看得见的：「力」被钮的左边框**劈开**，其后的「+3」整段看不见（玩家读到「…· 战力」，而"战力+几"才是这行重点）。**先查历史裁决再动手**：L30876-30877【R22/R32】明写钮是「原位**盖住**节点名/描述尾部」，x 195→238 是**量过** 18px **名字**的（名尾 492>455）⇒ **钮压右侧是有意设计，不动钮**；R32 漏的是这行 **14px 统计行**，而「盖住尾部」与「把一个字劈成两半」不是一回事（同族：v1.211d 章题条压住 16px 字上沿）。修法：这行把同一个属性名写了两遍（「基伤 +0%→基伤 +3%」）⇒ 去掉第二遍省约 32px，而缺口只需约 20px ⇒ **信息不减、字号不动、钮位不动** | v1.211i `69a7a14` | 本次 push 触发重拍（⚠ 只动 `game/`+`dist/` 的 push **不会**触发 art-shots —— 本轮实测：`69a7a14` 触发了 **10** 个 run 而 art-shots **不在其中**，与 §7.3 记的触发条件一致）。**验收（可量的）**：裁 `175,804,100,30` @8× ⇒ 统计行尾部**不得再被钮的边框切开**，且钮左侧应留出空隙（预期该行由约 190px 缩到约 158px，可用约 170px） |
| — | ⚠ **时间列口径（第 136 轮实测发现）**：表头写「时间(UTC)」，但 **L43–L49 那几行用的是本地时间（UTC+8）**，比 UTC 晚 8 小时 —— 判据：它们写的是 `2026-09-25 01:5x…05:0x`，而当时**实测 UTC 才 `2026-09-24 20:06`**，未来时间不可能是 UTC。上面这条新行起按**实测 UTC** 写。 | — | — |
| — | ⚠ **①b 的已知局限（本轮实测）**：它比的是"截图的 git 时间 vs 母版时间"，所以**只改注释/代码、不动像素的提交也会被它标成 STALE** —— 那种情况下图**本来就该不变**。判读顺序：先看本表（这批图对应哪一版、差哪几处改动），再看 ①b 的时间差；**时间差单独不足以判定"图过时"**。 | — | — |
| — | ⚠ 教训（第 114 轮实测两次）：① `art-shots` **只认 `_shots/**` 的改动**（改了画面就必须动一次本目录，否则那一轮不会重拍）；② 云端重拍会**和我同时往 main 推** ⇒ 两边分叉、`pull --ff-only` 会被拒，正确姿势是 `git pull --rebase origin main` 再推。 | — | — |

> 为什么记这一栏：①b 巡检只会说"截图落后几小时"，**说不出"这批画对应哪一版、差的是哪几处改动"**。
> 有了这张表，"能不能拿这批图下结论"就不用靠猜 —— 与 `MEMORY.md` 里"环境事实必须带复核日期"同一条道理。

