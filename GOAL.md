# GOAL

> 本文件每轮注入 → **必须精简**。历史阶段归档在 `.workbuddy/memory/GOAL-history/`（不删，可查）。
> 只保留：目标 / 状态 / 关键约束 / 已完成（一行一条）。

goal: 推动《萌兽消消岛》v1.162 的**内容**提升（看得见 / 听得见 / 玩得出来）——当前重心：**美术**
status: active

## ✅ 已完成（一行一条；细节见归档与报告）

- 六维审查全量化 · 外部基准对照（pixijs 48.2k★ / phaser 40.3k★）
- **AI 美术 136 项**（12 类）· 覆盖审计 GAP=0 · **受击帧 21/21 · 死亡帧 21/21**
- **151 张贴图去白底**（载荷 −5.7%，实机白方块全部消失，提交 `58a33816`）
- 内存：画布像素 **55.45MB → 3.79MB（−93%）** · 死数据镜像回收（−1.087MB）
- UI 巡检 10 界面 · 音频实测（66 SFX / 10 BGM）· **三形态横向验收一致**
- 战场纵深感（底色垂直渐变，零新增元素/零体积）· 乌鸦可读性修复（亮度 51→76.5，回到同类区间）
- **云端 CI 全线打通**：repo `yb-dey/mengshou-survivor`（private）· 双通道验证 ·
  美术审计 · 抠图流水线 · **不依赖 VPN** · 本机零算力
- 首屏传输 −41% · 闸门三项 PASS · 7 个核验/工具脚本沉淀进技能
- **【v1.168】音频映射缺口修复**：`CONFIG.audio.gains`(19 键) 与 `AUDIO_ASSET.sfxFiles`(15 键)
  **差集 4 键**（CARDSHOW/CHEST/EVENT/REVIVE）→ 有合成器、无映射 → `tryLoadLocalFiles()` 永远
  拿不到外采文件、**静默走程序化兜底且零报错**，覆盖 **10 个埋点**（全部 5 个章事件提示 + 复活
  + 开箱 + 亮卡 = 玩家最常听到的节点音）。补 4 键 + 用产品同源合成器离线渲染 4 个 wav
  （31.8K/31.8K/44.1K/51.2K B）→ **19/19/19 三者一致**；新增 GATE `ci/assert-audio-map.mjs`
  （4 检查 + 4 阴性对照）并接进 `verify-dist.yml`；`_qc/sync-deploy.js` 修正指向废弃副本的路径
  （deploy 一直在同步旧产物、"缺失 0"从旧产物里数 → 恒真）
- **【v1.169】音频映射断言升级 → 4 方一致 + 抓出「守卫自己坏了」**：
  给 `ci/assert-audio-map.mjs` 加**第⑤条「消费方口径」**——`DATA_SFX`（**66 个游戏事件**）
  引用的每个 buffer 必须在 `gains` + `sfxFiles` 里。这是**唯一能抓「两张表一起漏」**的一条
  （①~④ 都在声明表之间互证，两表同文件、一起漏就一起错）。实测 **19/19/19/19 四方一致**；
  阴性对照 4 项 → **6 项全绿**；云端 run `35591688036` **PASS**（并核验 CI 日志第⑤条真跑）。
  ⚠ 写第⑤条时**守卫自己踩了同型坑**：`indexOf('DATA_SFX')` 命中的是**第 1377 行注释**
  （真定义在 8242 行）→ 解析出 0 条 → 检查静默永不触发；**靠阴性对照没红才发现**。
  → 沉淀：「`indexOf(名字)` 永远不够，锚点要带定义语法」+「解析出 0 条本身就是 FAIL」
  （解析失效 ≡ 守卫失效，两者都全绿）。技能 `ai-art-pipeline` 升 **1.5.7**
- **【v1.169】同一类坑的更深一层：加载清单硬编码 → 16 个音频资产永远不被 fetch**：
  `tryLoadLocalFiles()` 硬编码 `localBgm`(6 键)/`localSfx`(7 键)，而 `AUDIO_ASSET` 声明
  `bgmFiles` **10** / `sfxFiles` **19** → **12 个 SFX + 4 个 BGM**（`bgm_horde`/`abyss`/`win`/`lose`）
  **永远不会被 fetch**，静默走程序化兜底、零报错。v1.168 修的是"声明表漏键"（补完声明表也
  不算完），v1.169 修的是"**加载清单漏键**"（还得有人真的按声明表加载）。
  修法：删掉手抄副本，改 `for (id in AUDIO_ASSET.bgmFiles/.sfxFiles)` —— **结构上不可能再漂移**。
  **fetch 覆盖面 13/29 → 29/29**。断言加**第⑥条**（加载清单必须遍历声明表），阴性对照
  **4→7 项全绿**；云端 run `35592844855` **PASS**（核验 CI 日志第⑥条真跑）。
  ⚠ 同一轮内**第二次**踩"名字匹配 ≠ 真身"：`indexOf('tryLoadLocalFiles')` 命中的是 CONFIG 块
  注释（idx 34712），真函数体在 idx **518770**（相隔 484KB）→ 已改带定义语法的正则

- **【v1.170】双重缩放：同一个 gain 被应用两次（同类第四例）**：
  `ci/render-missing-sfx.mjs` 又抄了一份 `GAINS` 表 → 19 条中 **12 条**与真身
  `CONFIG.audio.gains` 漂移（`CARD 0.62↔0.70`/`HURT 0.62↔0.72`/`BOSS_DIE 0.70↔0.80`/`WIN 0.68↔0.80`…）。
  该表的值被**烘焙进文件峰值**（`normalize(raw, 0.708*gain/0.708)` ≡ 峰值=gain），
  而运行时(12593 行)**又乘一次** `CONFIG.audio.gains[sid]` → **同一增益应用两次**，且两次取
  **不同版本的值**，误差非线性叠加。铁证：`sfx_fire.wav` 峰值/gain = **2.168**（全场唯一离群，
  因它是**从未重渲**的历史资产，gains 0.50→0.32 后仍留旧标度）；其余 18 个比值聚在 0.85–1.11。
  修法：删掉那张手抄表，改为与游戏 `_renderAll`(12173 行)**同口径 —— 只做平峰值归一**
  `peakNorm=0.7` → 程序合成与"外采 wav"两条通路行为一致，**响度层级只由运行时 gains 决定
  （单一权威）**；19 个 SFX 全部重渲，峰值统一 **0.7000** ✅
  断言加 **检查⑦** + 阴性对照 **H/I**（共 **9 项**全绿）；云端 run `35595331532` **PASS**。

- **【v1.170】同类第五例：陈旧检测"看不见内容变化"（最隐蔽的一个）**：
  `build-dist.js`(98 行) 与 `sync-deploy.js`(92 行) 都用 **`size === size`** 判定"已同步"。
  重渲音频后时长/格式不变 → **字节数完全一样** → 判等成立 → **拷贝被静默跳过**，
  dist/deploy 永远留旧内容，而脚本打印"新拷 0 个 ✓"看起来一切正常。
  → 改为**逐字节比较**。铁证：修复前"新拷 1 个"，修复后"**新拷 18/19 个**"。
  ⚠ 这是本项目"手抄副本"病根之外**第二条独立病根**：**用弱等价判据做增量同步**。

- **【v1.170】⚠ 判据自纠：参考量必须与现象同尺度（我自己犯的错）**：
  新建 `sfx-intent-vs-real.py` 第一版**逐对比较 `gain` 与文件响度** → rho=0.491、21 组"反向对"。
  但二者**不是同一尺度**：`gain` 是档内调色，响度是"能量×时长"；`SFX_BOMB`(0.66) 实测比
  `SFX_REVIVE`(0.62) 轻 8dB 是**合法设计**（短促闷响 vs 绵长上扬，峰值同为 0.7 时能量差就是大）。
  逐对比较把合法设计报成 21 组反向对，**掩盖了唯一真问题**。已改为**档均值**（与 1420 行设计
  注释同尺度）并加阴性对照（层级正确必 PASS / 塌陷必 FAIL）。
- **【v1.175】win/lose 感知不可分修复 + 音频判据自我纠错**：情绪对立极点却是 171 对里最像的一对
  （距离 0.068）→ 结构去同构 **86%→29%**（补"绽开/沉降"相反修辞）；同时把"感知距离"判据
  **校准后重立**为**双轨合议**（结构同构率 + 感知距离，同时告警才判缺陷）→ `ci/sfx-distance.mjs`
  8 条自测全绿 + 硬门禁，云端 run `35624402620` success。详见第十二类病根。
- **【v1.177】SFX 层级门禁**判据尺度**又一次**纠错（**"时长混进响度"**）+ cardshow 双目标冲突修复**：
  换手势后 cardshow RMS 0.0693 → 用 30ms 滑动窗口径实测它是 **prio3 档最轻**、还低于 prio1 的 hit
  → 门禁报 "MID−FREQ = −0.3dB 塌陷"。**但深挖后发现游戏设计完全正确**：
  `CONFIG.audio.prios` 写的 **5 级纵向层级**在**起音峰值**上严格单调（win20/30/50ms 三档全单调）
  → 错的只是判据：①压成 3 档（粒度不够）②用**全段 RMS**（1.35s 的 lose 天然比 0.20s 的 hurt 响）。
  已改为 **5 级分档 + 30ms 滑动窗峰值响度**，阴性对照 **2 → 4 条**（含"时长无关性"回归守卫）。
  同时 cardshow 修"过轻"：**⚠ 加 sustain 会撞 start**（同构率 0.571→0.857，实测踩过）→ 改用
  **音数/节奏密度**（高音区 4 音密簇 784/1046/1318/1568@0.07s）→ RMS **0.1022**、同构率 **0.000**。
  云端 `35628253350` / `35628886856` 双 success。

### 第六类病根（v1.170，音乐维度）：**同档 peer 不等重 —— "等响但没身体"**

- **发现**：`win` 是 10 首曲子里**唯一没有任何低音层**的（`lose` 有注释明确的 A2 衬底、场景曲都有）。
- **量化（<200Hz 能量占比）**：win **5.8%** vs lose **31.9%**（差 26 个百分点），
  而峰值/RMS/峰均比几乎相同（0.708 / 0.110 vs 0.112 / 16.1 vs 16.0dB）
  → **听感等响、但 win 没有"身体"**；手机小喇叭（放不出 110Hz 以下）上更明显。
- **修法**：补它**自己的调性根音**（不抄 lose 的暗色 A2，那是失败曲的性格）：
  C2(65.41) 胸腔层 + C3(130.81/132.0 **微失谐对**) + C4(261.63) + G3(196.0)。
  - ⚠ **第一版只放一条 130.81Hz sine @0.14 → 5.8% 只抬到 7.4%，几乎无效**：
    130.81 与"和弦里已有的 C3 泛音"仅差 13.6Hz，同相叠加互相抵消 → 改**微失谐对**才攒住能量。
  - ⚠ **第二版发现中低音空档**：melody 从 C5(523) 起、低音只到 C3(131)，
    中间 C4(262) 空一节 → 实测 `lowmid(250-500Hz)` 仅 **0.02%**（lose 72.6%）。
    补 G3/C4 后 → **2.2%**。
- **结果**：`<200Hz` 5.8% → **27.1%**（lose 31.9%，差距 26→4.8 点）；
  `<100Hz` 1.5% → 15.8%；**峰值仍 0.7080 不变**（"响度不升"原则）；
  频谱重心 **1120 → 872 Hz**（10 首里从"最偏高频"往队伍中间靠）。
- **铁律（新增第六条）**：**同档 peer 必须做「等响度下的结构对照」** ——
  只比峰值/RMS 会得出"两者一样"的结论，**必须再比频段分布**（低频占比 / 频谱重心），
  才能发现"等响但少一层"这种**结构性不等重**。
- ⚠ **判据口径提醒**：`bgm-spectrum.py` 报 win 为 OUTLIER 是**正确的**（它确实仍在分布边缘），
  但**改动方向是"往队伍里靠"**（重心降 248Hz、低频 +25 点）→ **离群标记 ≠ 改动无效**，
  要对比**改动前后**才能判方向。（同"指标变好≠看着变好"的另一面。）

#### 第六类病根的**第二个实例**：SFX `sfx_win` / `sfx_lose`（同一病根，跨资产类）

- **发现（规模化排查）**：把 6 个 INFREQ 事件全排成"分带表"后，`sfx_win` 是**唯一**
  `low(<200Hz)` + `lowmid(200-500Hz)` **双 0.0%** 的（其余 INFREQ 均有低频底）；
  它却是"局终胜利"这种最该有"落定感"的音。
- **修法**：给两首终局 jingle 补**低音基础层**（与渲染器逐字一致）：
  `sfx_win` = C2(65.41)@0.30 + C3(130.81)@0.20；`sfx_lose` = A1(55.0)@0.28 + A2(110.0)@0.18；
  同时把旋律层 gain 从 0.84/0.82 抬到 **0.95/0.93**（补偿低音抢走峰值后的归一损失）。
- **结果**：`low` 0.0% → **~7%**；响度 -17.1 → **-17.6**（win）/ -16.5（lose）；
  档位落差 **INFREQ−FREQ 保住 3.6 dB**（门限 2.0）。
- ⚠ **新坑：低音层会抢峰值 → 归一后整体变轻 → 削弱档位差距。**
  首版 bass gain 0.78/0.62 时 `low` 冲到 15.2%，但**响度掉到 -18.3、档位差距缩到 2.5dB** ❌
  （低频能量大 → 平峰值归一把它压得更小 → INFREQ 档中位被拉低）。
  → 最终 gain 是**扫出来的**（mel 0.95 + bass 0.30/0.20 才两头兼顾）。
- ⚠ **报告文案误读自纠**：门禁提示"高频事件可能偏吵"是**误读** ——
  实为**低频档被压轻**（MID/FREQ 档中位数完全没动），不是 FREQ 变响了。
- **沉降**：铁律六已写进技能 `ai-art-pipeline` 1.6.0；本条是它在 **SFX** 维度上的复用验证。

### 第七类病根（v1.171，性能维度）：**"验证覆盖不到"而非"验证通过"——加载帧时盲区**

- **发现**：`ci/verify.mjs` 只在**开局轻载**下量 fps（8s 窗口，场上怪个位数）。
  而本作最重的场景是**怪潮**（ch6 无尽可达 `CONFIG.maxEnemy=200` 同屏）——
  **轻载 60fps 完全不能证明重载不卡**。这是"验证覆盖不到的地方"，不是"验证通过的地方"。
  与前六类病根的区别：前六类是"产物有缺陷"，这类是**"守卫看不见"**（守卫自己坏了同族）。
- **补法**：新增 `ci/stress-perf.mjs` + `MENGSHOU_DEBUG.stress()` 只读证据口，接入 `verify-dist`：
  1. 先 `seek()` 推进到**怪潮窗口**，`enemyCount` 爬到阈值才继续（**先证有怪**）；
  2. 再量**帧时间分布**（p50/p95/max）—— 量尾部而非 fps 均值（**玩家感知的是帧时间尾部**）。
- **判据两条硬约束（都是"防自欺"）**：
  - **先证有怪再判帧率**：不这样做，"帧率好"可能只是"没怪"（假阴性）。
    首次运行正是靠这条**拒绝给出 PASS**（34 怪 < 阈值 → 判"样本无效"）。
  - **阴性对照**：注入 250ms×3 主线程阻塞，判据必须报 FAIL。
- ⚠ **阴性对照本身踩了一次"判据选错分位"**：注入的阻塞是**稀疏**的（3 帧/66 帧 ≈ 4.5%），
  **p95 落在未阻塞帧上**（实测 19.9ms）而 **max 正确捕获 269ms** →
  用 p95 会把**有效探针误判为失效**（"守卫自己坏了"的反向误判）。**已改用 max**。
- **阈值按 CI 实际标定**（不是拍脑袋）：
  - 同屏怪数下限 **45**：ch1 怪潮声明 countMin 72，但**同屏存活**低于灌入总数（怪会被击杀 +
    软件渲染帧长拉长灌入节奏）→ CI 实测峰值 **47**；45 ≈ 实测 ~95%，调高会在 CI 假阴性。
  - p95 预算 **50ms**：CI 跑 **swiftshader 软件渲染**（无 GPU），绝对值比真机慢数倍
    → 此阈值是**回归哨兵**（改恶化了会掉出预算），**不是"达到 60fps 的证明"**。
- **结果（云端 run 35605956457 PASS，v1.171）**：47 怪同屏 · p50 **30.8ms** · p95 **34.6ms** · max 49.3ms ·
  平均 **36.4fps**（软件渲染）· 未捕获异常 **0**；阴性对照 max **281.3ms** ≥150 ✅
- **判据**：这是**第一个"性能维度"的客观门禁**，把"11 章内容是否卡"从"感觉"变成"数字"。

### 第八类病根（v1.172，持久化维度）：**"静默摧毁"——存档管线零覆盖**

- **发现**：本作有**唯一持久化通道** `SAVE_SCHEMA`(33 字段/schemaVersion 7) → `validateSave` →
  `clampSaveRanges` → `migrateChain[1..6]`，外加 `exportSaveText`/`importSaveText` 的 `{d, chk:djb2}` 包装。
  **存档损坏会摧毁玩家全部进度，而且是静默的**：`Storage.load` 的 `catch` 直接落 `defaultSave()`——
  没有报错、没有崩溃、没有日志，玩家只看到"进度没了"。
  而 `grep -rln SAVE_SCHEMA ci/` **返回空** → 这条通道**零 CI 覆盖**。
  属第七类病根（"验证覆盖不到"）在**持久化维度**的又一实例。
- **补法**：新增 `ci/save-integrity.mjs` + `MENGSHOU_DEBUG.save()` 只读证据口，接入 `verify-dist`。
  四组断言：**A** 结构完整（字段集精确相等、往返自检）· **B** chk 篡改拒绝（**带正对照**）·
  **C** 11 项脏值注入必须被钳制 · **D** v1 最小档迁移到 v7 且**不吞旧值**。
- **两条"防自欺"硬约束**：
  - **B 组必须同时有正对照**：只证明"篡改的不过"是不够的 —— **一个永远返回 `false` 的实现也能通过那个断言**。
    （"永远通过的守卫"的反面同型错误：**永远拒绝的守卫同样不是守卫**。）
  - **A 组的往返判据自带阴性对照**：另造一份"必然不等价"的样本喂给同一判据，它必须 `false`。
- ⚠ **本轮抓到两个真缺陷（都是"守卫自己坏了"，不是游戏缺陷）**：
  1. **证据口 roundTrip 判据写错**：`exportSaveText()` 会往 body **注入 `schemaVersion`**，
     而运行时 `saveData` **从不存该键**（只在写档/导出时补）→ 拿两者直接对比**永远不等价**。
     被 CI 抓到（报告 `FAIL(undefined)`）。修法：两侧**都先过 `validateSave` 同形化**再比。
     > 教训：**"我加的探针"也要被验** ——离线模拟用 `Buffer` 替代了浏览器 `btoa`，
     > 路径不等价 → 本地没复现，**靠云端真跑才暴露**。
  2. **`continue-on-error: true` 让 FAIL 显示成 success**：体检步骤 `continue-on-error: true`
     （本意是"失败也要上传报告"），代价是**步骤永远绿** —— 报告里写着 `FAIL`，CI 却打绿勾。
     这是本项目反复踩到的"**守卫永远通过**"反模式（永远绿的守卫比没有守卫更危险）。
     修法：补一个**门禁收口**步骤，读 `steps.*.outcome`，任何一项非 `success` 就 `exit 1`。
     → **顺带把 `verify`/`stress`/`save` 三个门禁一起收口**（此前它们三个都是永久绿）。
- **结果（云端 run 35609728401 PASS，v1.172）**：结构 33/33 键 · missing 0 · extra 0 · 迁移链 6 节点 ·
  往返自检 **ok**（+ 阴性对照"能检出不等价" ✅）· 篡改拒绝 3/3 ✅ · 钳制 **11/11** ✅ ·
  v1→v7 迁移 missing 0 且旧值（coins 1177 / muted true / guideStep 2）**全部保留** ✅
- **判据**：这是**第一个"持久化维度"的客观门禁**，并且**首次把门禁自身的有效性纳入门禁**。

### 第九类病根（v1.173，元维度）：**门禁自身名不副实 ——"守卫永远通过"**

- **发现**：v1.172 我在存档体检报告里写了 `## **FAIL**`，GitHub 步骤却是**绿勾**。
  根因 `continue-on-error: true`（本意"失败也要上传报告"）→ 它让步骤失败**不影响 job 结论**。
  全量扫 8 个 workflow，**同类共 3 处**：
  | workflow | 症状 | 处置 |
  |---|---|---|
  | `optimize-art.yml` | 步骤名「抠图后**必须**仍然 PASS」，却 coe + **无收口** | 补「门禁收口」 |
  | `death-frames.yml` | 步骤名「③ 云端双通道验证（**门禁**）」，同样无收口 | 补「门禁收口」 |
  | `screen-sweep.yml` | 可读性体检 coe，**但属有意弃权**（判据已证伪、仅留痕） | **加注释**明确"不补收口" |
  → **"永远通过的守卫"比没有守卫更危险**：它让人**以为**验过了。
  与第七/八类的区别：前两类是"覆盖不到"，这类是**"覆盖了但不生效"**——更隐蔽。
- **补法**：新增 `ci/gate-audit.mjs`，作为**硬门禁**（自身**不带** coe，否则它就在犯自己审查的错）
  接入 `verify-dist`，排在**第 5 步**（构建后立刻跑：门禁本身不可信时，后面跑再多也没意义）。
  四判据：① 声称门禁 + coe → 必须有收口；② 声明"不作门禁"→ 允许 coe（合法弃权）；
  ③ 收口引用的 `steps.<id>` 必须真实存在；④ `continue-on-error: ${{ }}` 表达式 → 不误判也不放行。
- **⚠ 判据自身的 3 个 bug 全被它自带的对照抓出**（本轮最有价值的记录）：
  1. `CLAIM` 在「不**作门禁**」里命中"门禁"→ 把**正确**写法误判"自相矛盾"（3 处假阳性）
  2. 剥离否定短语时占位符写成「〔弃**门禁**〕」→ **里面仍有"门禁"**，剥离等于没剥
  3. 判断顺序错：`claims && disclaims` 对否定式必然同时为真 → **必然误报**
     → 修法：**先判是否明确弃权（弃权=跳过，不追问），再判是否声称门禁**
  对照完整性：**阴性 3 个 + 阳性 3 个**（阳性 B 专守"否定式误判"）。
- **⚠ 第二个真 bug：`ci/out` ENOENT ——"在我机器上是好的"**
  云端首跑（run 35612499844）直接崩：`ENOENT: open 'ci/out/gate-audit.md'`。
  全新 checkout 里 `ci/out/` **不存在**；**本地跑过所以目录早就在** → 本地 100% 绿、云端 100% 崩。
  修：`fs.mkdirSync(OUT_DIR, {recursive:true})`。
  **顺手扫出同类隐患 2 处**：`ci/screen-analyze.py` / `ci/text-legibility.py` 写 `ci/out` 却无 mkdir，
  只因 `screen-sweep.yml` 里 Playwright 那步恰好建过目录才没炸 —— **靠运气的顺序耦合**，两处均补 mkdir。
- **⚠ 第三处改进**：崩溃后三个体检全变 `skipped`，收口报"门禁未通过"→ **误导排查方向**。
  修：收口区分 `skipped|cancelled` 并提示"优先检查更早步骤是否崩溃"。
- **结果（云端 run 35612786456 completed/success，28 步全绿）**：
  自审输出 `## **PASS** — 门禁名实相符 ✅`；产物 897,396B 上传成功
  （**"No files were found" 警告消失** → 反证 mkdir 修复生效）。
  **故障注入实证**：给「门禁自审」自己加 coe → 判据立刻 `EXIT=1`，
  点名 `verify-dist.yml L29 — 名不副实 ... 没有任何收口步骤读 steps.ga_fault.outcome`。
  → **判据能抓住它自己犯错**，这才叫真门禁（不是看报告，是真打洞）。
- **判据**：这是**第一个"元维度"门禁** —— 不检查产物，**检查检查器本身是否诚实**。

### 第十类病根（v1.173，内容维度）：**"淡刀子"文案系统的一半条目静默消失**

> 前九轮（v1.165→v1.172）都在做**验证硬化**（把门禁做真、把覆盖补齐）。
> 这一轮是**第一个真正的"内容"维度工作** —— 用户要的是"看得见 / 听得见 / 玩得出来"，这是"看得见/读得到"。

- **发现**：本作有一个**已建立的文案系统**「淡刀子」——`DATA_LORE` 定调
  `kicker: "薄设定 · 淡刀子 · 全程可跳过"`，核心意象是**冷/暖/巢/热/旧名字/走散**。
  图鉴与关于页用 `filter(e => !!e.knife)` 把它渲染成一个"刀语录"面。
- **病根：那个 `filter` 是静默的。**
  ```javascript
  // 行 35266 / 35269
  pets:    DATA_BEAST.filter(function (d) { return !!d.knife; }),
  enemies: DATA_CODEX_ENEMY.filter(function (e) { return !!e.knife; }),
  ```
  缺 `knife` 的条目**无声消失**；玩家点该条目后只回退到一句通用提示
  （行 30442 `msg = (en && en.knife) ? en.knife : "点分组再点条目 · 读招字 + 出处一眼能对上";`）。
- **实测缺口**（先量数据再下结论）：
  | 表 | 总条目 | 有 knife | 缺口 |
  |---|---|---|---|
  | `DATA_CODEX_ENEMY`（敌人图鉴） | 21 | **5** | **16 条不可见** |
  | `DATA_BEAST`（跟宠） | 7 | **2** | **5 只不可见** |
  → 一个**已经写进定调的文案系统**，实际只对 **7/28** 个条目生效，其余 21 个在文案面上等于不存在。
- **归类**：与 v1.168/v1.169 的**音频缺口同型** ——
  "**声明了系统、但多数条目没填/没接**"，且**静默降级**（不报错、不崩溃，只是没有）。
  也解释了为什么 `content-gap-scan.mjs` 报「无缺口」：它扫的是**键存在性**，
  而这里键是**可选**的（`!!e.knife`）→ **存在性门禁天然看不见"可选项大面积缺失"**。
- **处置（补全 21 条文案 + 建门禁）**：
  1. **补全 21 条 knife**（16 敌人 + 5 跟宠），写成 `, knife: "..."` 追加在行尾 `},` 之前。
     语感**对齐既有 34 条**：中文 10–22 字 · 第二人称凝视 · **转折而非斥责** · 落点在"冷/暖/巢"而非胜负。
     例：`rabbit: 直着冲不是莽，是认准了这边还有热气。` /
         `badger: 贴着不放不是凶，是它身后还有一窝要挡。`
     ⚠ **严守既有语气**：`DATA_LORE` 第一句是"走散的不是恨，是冷" → 通篇**不许出现恨/仇/征服/胜利**。
  2. **新增 `MENGSHOU_DEBUG.lorePack()`** 作为**运行时证据口**：同时报「全量条数」与「入匣条数」。
     **必须两侧都报** —— 只报入匣数看不出缺口（`5` 和 `21` 都是"有值"）。
  3. **新增 `ci/lore-coverage.mjs`** 门禁（零依赖静态解析），接入 `verify-dist` 作**硬门禁**。
- **⚠ 判据连踩两次"太严"（本轮最有价值的教训）**：
  | 版本 | 判据 | 后果 |
  |---|---|---|
  | v1 | 转折写死成「不是」 | **把项目自己的正典判成不合格**（蜜罐 / 老板2 / 老板3 / 绒绒羊） |
  | v2 | 扩到 5 个转折词 | 「壳越滚越厚，里面已经喊不出声」「把潮水吞回去…」**仍不合格**（它们用**意象对照**，不用转折词） |
  | v3 ✅ | 改**可穷举**的两条：长度包络 10–22 + 虚词骨架 `[把是不却只换让为怕给]` | 34 条既存文案**全部通过** |
  - **结论：按"转折词白名单"判是错误方向** —— 永远补不全，越补越像在给判据打补丁。
    改判**可穷举**的维度（长度包络 + 是否含虚词骨架），才能既挡住"纯陈述句"又不误伤既有正典。
  - 同时保留判据 D **消费侧自证**（游戏里必须真存在读 `knife` 的渲染点）：
    **填了没人看 = 白填**，这条把"内容"和"消费"绑在一起。
- **结果（v1.173）**：
  - 三形态一致：母版 / dist / deploy 均 **v1.173 · 敌人 21/21 · 跟宠 7/7**（1,717,046 B 分发口径）
  - `sync-deploy` 校验 **必需缺失 0** · 音频声明 29 个缺失 0
  - 云端 run `35615733224` **completed/success**，新步骤输出
    `| DATA_CODEX_ENEMY | 21 | 21 | 0 |` / `| DATA_BEAST | 7 | 7 | 0 |` / **`PASS`**，
    阴性 3 + 正 2 对照全绿
  - **运行时实证**（Edge + CDP，非静态）：
    `lorePack() = {version:"1.173", enemyTotal:21, enemyWithKnife:21, petTotal:7, petWithKnife:7, enemyInPack:21, petInPack:7, missingEnemies:[]}`，
    `pageerrors = 0` —— **静态"字段存在"与运行时"真的进了匣"两侧都证**。
- **判据**：这是**第一个"内容维度"的客观门禁**（此前 10 轮全是工程/验证维度）。
  同时暴露一条更普遍的规律：**存在性门禁看不见"可选字段大面积缺失"** ——
  凡是 `!!x.field` 这类**可选消费**，都得单独建"覆盖率"门禁，而不是靠 `content-gap-scan`。


### 第十一类病根（v1.174，内容维度·美术侧）：**配方分支只有"叫什么"，没有"什么时候选"**

> 承 v1.173 同一条线：都在修「结构里有位置、但内容没填，而且不报错不崩溃」。
> 区别是 v1.173 修的是**图鉴条目读不到**，这轮修的是**玩家做不了决策**。

- **发现**：配方图鉴（fork tab）里，每条进化分支的数据只有 `feel`（3–4 字，如「叠弹暴雨」「七向扇铺」）
  —— 它描述的是**这条分支长什么样**，不是**什么局面该选它**。
  `DATA_CODEX_FORK` = 10 组 × 2 分支 = **20 个决策点，全无选择依据**。
  → 玩家看到的是一排名字 + 一行形态词，**选哪个纯靠猜**。
- **病根与 v1.173 完全同型**：卡片照常画出来、不报错、不崩溃，**只是那一行根本不存在**。
  `DATA_CODEX_FORK` 的分支对象 schema 里**压根没有"用途"这个字段**，所以
  任何存在性扫描（`content-gap-scan`）都不会报 —— 它扫的是**已声明键**，而这里缺的是**没声明过的键**。
- **处置（补 20 条 pick + 建门禁）**：
  1. **补 20 条 `pick`**（每条 = 一行「什么局面选它」），语法为 `, pick: "..."` 追加在 `feel` 之后。
     语感与既有 `feel` 一致：**先给局面、再落到"选它"**，中文 8–12 字，例：
     `acornstorm: 要铺满一条走廊时选它` / `fluffguard: 想让伤害自己飞出去时选它`。
     ⚠ 严守本作定调：**不许出现征服/称霸/统治/秒杀/无敌/最强/必须/绝对**（与 v1.173 同一张禁用词表）。
  2. **`forkCodexOf` 透传 `pick`** —— 声明了但没透传 = **填了没人看**（v1.173 判据 C 同一条纪律）。
  3. **fork 卡渲染第二行**：每分支 = 「主行（req→name·feel）+ 选它当口」两行。
  4. **新增 `ci/branch-pick.mjs`** 门禁（零依赖静态解析），接入 `verify-dist` 作**硬门禁**。
- **⚠ 布局上踩的坑（记下来防重犯）**：
  | 尝试 | 结果 |
  |---|---|
  | 卡高 `codexForkH` 96 → 118，给 pick 腾地方 | **配方 tab 内容底 1462 > 1280 视口** → 底部成档卡被裁 + 返回钮压卡 |
  | 回退卡高 96，pick 挤在同一张卡内（pitch 22→30、字号 10px） | ✅ 清晰不重叠 |
  - **根因**：配方 tab 是「分叉(5行) + 成档(2行) + 进化(3行)」**三段串排**中的一个 ~416px 高的 tab，
    卡高每 +22px 就把整 tab 顶出去。**"给新内容加高度"在这里是错的** —— 该做的是让新内容适配既有高度。
  - **顺带修出一个 pre-existing bug**：`codexContentBottom()` 的 fork 分支**只数分叉卡 5 行，
    完全漏掉同 tab 下方的成档(7) + 进化(9) 两段** → 少算 **416px**。
    对照 `_bak_v173.html` 确认 **v1.173 就已存在**（不是本轮引入）。
    这类"面板高度按最坏情况算漏一段"是本项目第 ② 类系统性缺陷的又一例。
- **⚠ 判据长度下界的来历（防止"为了让门禁通过而放水"）**：
  首跑 FAIL：`pinecone/needlespray: 字数7不在 8–20 → 杂兵成排时选它`。
  **处置顺序很重要**：先量 20 条的实际分布（**实测 8–12 字**），确认 7 是**真实的离群**，
  所以①改文案（「杂兵排成一行时选它」9 字）②才把下界从 8 调到 7（= 实测最短 8 留 1 字容差）。
  **不是**直接把 8 改成 7 放过它 —— 判据包络必须从真实产物量出来，松它等于放水。
- **⚠ 顺手修掉一个"假警报哨兵"（`ci/stress-perf.mjs`）**：
  本轮 CI 首跑 `门禁收口` 报红，追下去是 `stress = failure`（p95 50.6 > 预算 50）。
  **先量数据再下结论** —— 拉四次同代码 run 对比：
  | run | p95 | 同屏怪数 | 结论 |
  |---|---|---|---|
  | 35618518251 | **18.3 ms** | 45 | PASS |
  | 35618531812 | **29.9 ms** | 45 | PASS |
  | 35618553577 | **50.6 ms** | 46 | **FAIL** |
  | 35615733224（v1.173 基线） | 21.1 ms | 46 | PASS |
  → **同代码、同怪数，p95 极差 2.8×**，而预算 50 正好卡在观测带上沿 → **约 1/5 概率随机判红**。
  **"偶尔红"的哨兵比没有哨兵更糟**：真回归来了会被当成又一次抖动而忽略。
  修法（两条一起）：**预算 50→80**（≈观测最大值 1.6 倍，只抓数量级恶化）+ **采样窗口 5s→10s**
  （降方差；复验 run p95 = 23.6ms，采样 528 帧 vs 原 238 帧）。
  这与 v1.172「守卫永远通过」是**镜像问题**：一个是永远绿，一个是随机红，两者都失效。
- **结果（v1.174）**：
  - 三形态一致：母版 / dist / deploy 均 **v1.174 · 20/20 分支有 pick**（1,720,205 B 分发口径）
  - `sync-deploy` 校验 **必需缺失 0** · 音频声明 29 个缺失 0
  - 语法守卫（抽内联 script → `node --check`）**PASS**（4,205,568 B 内联）
  - 云端 run `35619663903` **completed/success**，新步骤输出
    `| 分支总数 | 20 | / | 有 pick | 20 | / | 缺 pick | 0 |` / **`PASS`**，阴性 4 + 正 2 对照全绿
  - 4 个文件远端↔本地**字节一致**（blob sha1 逐一对上）
  - `stress-perf` 复验 **p95 23.6ms（预算 ≤80）/ PASS**，收口步骤绿


### 第十二类病根（v1.175，音乐维度）：**情绪对立极点却是"最像的一对"——SFX 感知不可分**

> 承第六类（同档 peer 不等重）的**下半段**：v1.170 修的是"等响但没身体"（低频空白），
> 这轮修的是"有身体但没区别"——**赢和输听起来几乎是同一段音**。

- **发现**：`sfx_win` 与 `sfx_lose` 代表"赢了/输了"两个**情绪对立极点**，
  但它们在 29 个音效 **171 对里感知距离最小（0.068）**，判据 JND=0.210
  → **是全部音效里最像的一对**，而它们本该是最不像的一对。
- **根因不是旋律方向错**（方向是对的：win 上行 / lose 下行），
  而是**除音高方向外全部同构**：同 4 音 triangle 琶音、同节奏 `t0=i*0.15`、
  同时值 0.45、同 0.9s 长度、**同"完全没有低音层"**（v1.170 前）。
  → 人工对照母版源码 7 个设计维度：**同构率 6/7 = 86%**。
- **处置（方向不变、音色家族不变、起音纪律不变，只补"收束修辞"）**：
  | | win 补"绽开" | lose 补"沉降" |
  |---|---|---|
  | 旋律 | 4 音 → 4 音 + **3 个亮尾**（冲 C7） | 4 长音 → **8 短音**（步长 0.15→0.095，"更碎更泄气"） |
  | 频率 | 每音尾 **+2% 上扬** | 每音尾 **−1.5% 下坠** |
  | 独有层 | 亮尾 + 高八度微失谐 shimmer（3.6Hz 拍频）+ 五度高亮层 | 0.55s 起 **200→38Hz 长下滑 sub** + 暗色 saw pad + 散场气声 |
  | 时长 | 0.90 → **1.02s** | 0.90 → **1.35s**（拖下去，不收束） |
  → 同构率 **86% → 29%**（人工对照）；结构同构率判据实测 **0.429 → 0**。
- **⚠ 响度纪律（v1.170 铁律）**：只做**平峰值归一（0.7）**，响度层级只由运行时 `gains` 决定。
  逐层实测：RMS 0.1591→0.1587（几乎不变），ΔLUFS(win−lose) −3.25 → **−3.07dB**（差 0.18dB，听不出）→ 档位关系保持。
- **⚠ 本轮最大的方法论收获：一个判据的"自我纠错"（三次自我推翻）**
  主判据（12 维感知距离）报 `sfx_gem↔sfx_ui = 0.173 < JND 0.219` FAIL。**没有直接改音频**，
  先做**判据的分辨率校准**——拿 4 组"已知答案"的锚点对检验判据单调性：
  | 锚点对 | 8 带距离 | 20 带距离 | 期望 |
  |---|---|---|---|
  | 3kHz 噪声 ↔ 60Hz 低音 | 2.529 | 2.528 | 远 |
  | 同段音 + 1 半音 | 0.348 | 1.479 | 近 |
  | 同段音 + 3 半音 | **0.096** | 1.337 | 近 |
  | 同段音 + 5 半音 | 0.355 | 1.537 | 近 |
  → **8 带判据非单调**（0.348 ≤ 0.353 ≤ 0.096 ≤ 0.355，"差 1 半音"比"差 3 半音"还远）→
  **是判据分辨率不足，不是音频有缺陷**。改用 20 带后单调。
  再深查 gem↔ui：20 带下 0.562 = **1.38×JND 完全可分**（gem 1568/2093Hz 纯 sine vs ui 784/1175Hz triangle，
  **差一整八度 + 波形族不同**）→ **判据假阳性，判定推翻**。
- **第二条自我纠错（结构同构轨的假阳性）**：改判据为"结构同构率"后误报 `evo↔ui = 0.857`。
  实测两者距离 **1.142 = 5.2×JND**（唯一差异维 `dur` 0.46s vs 0.12s 差异极大）——
  **"一维大差异"足以区分**，同构率高不能单独定缺陷。
- **最终判据 = 双轨合议（两者同时告警才判缺陷）**：
  ```
  结构同构率 ≥ 0.8  且  感知距离 < JND  且  不在 ALLOW 白名单
  ```
  单轨告警一律降为**提示**。→ 建立 `ci/sfx-distance.mjs` 作硬门禁，含 **8 条自测**
  （含 A2「标定退化本身会被检出」、E3「同文件=1.0」阴性对照）。
  ⚠ 自测首跑 A 用例"未检出"→ 查出 **3 样本语料下 `floor(3×0.05)=0` 致 JND 退化为 min**
  → 扩到 N=12（`PAD` 背景样本表）并补 `info.p05Index` / `info.jndDegenerate` 字段。
- **⚠ 三个被淘汰的"走向"估计器（记下来防回退）**：
  | 估计器 | 为什么不行 |
  |---|---|
  | 自相关音高轨迹 | lose 中后段 sub 下滑 + saw pad + 气声三层叠加 → 锁到短 lag（实测 570ms 起 f=3150Hz、norm 0.97，**是真实成分但"单基频"假设失效**） |
  | 全带频谱重心 | lose 尾部**气声层**抬高整体重心 → 斜率 +0.0036，与听感"下坠"**相反** |
  | 低带能量占比(80–640Hz) | 低音床(55/110Hz 长持音)全程占能 → 占比恒在 **95–100% 饱和**，旋律走向被淹没 |
  | **✅ 旋律层重心带位（采用）** | 只在**半音栅格 220–2093Hz** 上算重心 + **按帧能量门控**（rel<0.15 不判）；床层不在栅格、气声不成峰 → 自动排除。实测 win **6.06→9.81（+3.76 上行）**、lose **6.03→0.56（−5.47 下行）** |
- **结果（v1.175）**：
  - 三条独立证据**同时成立**（`_qc/_verify-v175-final.mjs` exit 0）：
    ① 结构性 86%→29% ② 旋律走向相反且显著（+3.76 / −5.47 带位）③ 时长差 32%
  - 三形态同步完成 · `sync-deploy` **必需缺失 0** · 音频哈希 **29/29 一致** · `final-audit` **PASS**
  - 云端 `verify-dist` run `35624402620` **completed/success**，新门禁以**硬门禁**运行，
    日志确认 `## 自测: **PASS**`（8/8）+ `## 结论: **PASS**`（无双轨同时告警）

### 第十三类病根（v1.177，音乐维度）：**"时长混进响度"——层级判据第三次犯同一个尺度错**

> 与本文件已在案的 **v1.170（参考量必须与现象同尺度）**、**v1.174（stress-perf 门限贴观测带）**
> 是**同一个病根家族的第三次发作**。建议把这条读成一句话：
> **只要判据的两个量不是同一个物理维度，结论就会在临界处随机翻转。**

- **表面现象**：`ci/sfx-intent-vs-real.py`（硬门禁）在 CI FAIL：
  `MID − FREQ = −0.3dB`、`档位单调 ❌` → 报"档位层级塌陷"。
  触发者看似是 cardshow（换手势后 RMS 0.1143→0.0693）。
- **差点做错的修法**：把 cardshow 硬抬响度去凑 `MID − FREQ ≥ +0.2dB`。
  反推需要 RMS **0.155**（比开局音 start 的 0.104 还响）→ **会制造新的响度失衡**。**否决。**
- **真根因（两层，都在判据不在内容）**：
  1. **分组粒度不够**：设计真身 `CONFIG.audio.prios` 写的是 **5 级**纵向层级
     （受击/复活 5 > BOSS/开局/进化 4 > 升级/选卡/结算/击杀 3 > 宝石 2 > 开火/命中 1），
     门禁却压成 **3 档**（INFREQ/MID/FREQ）→ prio3 与 prio5 混在一档，档内跨度 5.9dB。
  2. **判据尺度错**：门禁用**全段**均值平方当响度 → **时长被算进了响度**。
     `sfx_lose` 1.35s 全段 −16.0dB vs `sfx_hurt` 0.20s 全段 −16.2dB
     → 体检读成"结算音比受击音更响"，而设计写的是 HURT(prio5) 该高于 LOSE(prio3)。
- **决定性证据**：改用 **30ms 滑动窗峰值响度**（对 0.07s 的 fire 与 1.35s 的 lose 都只问"最响的一瞬有多响"）后：
  | 窗口 | p5 | p4 | p3 | p2 | p1 | 严格单调 |
  |---|---|---|---|---|---|---|
  | 20ms | −8.03 | −10.78 | −11.30 | −12.15 | −14.08 | ✅ |
  | **30ms** | **−8.78** | **−11.77** | **−12.44** | **−13.64** | **−15.82** | ✅ |
  | 50ms | −10.48 | −12.98 | −14.13 | −15.83 | −18.04 | ✅ |
  → **游戏音频的层级设计完全正确**，三档窗口宽度结论一致（不敏感）。错的只是判据。
- **处置**：
  1. `ci/sfx-loudness.py` 新增 `window_peak_loudness()`（30ms 滑动窗最大 K 加权响度），
     与全段 `loud` **并存**（后者仍用于"高频 vs 低频"磨耳朵粗检，那是另一个人群语义）。
  2. `ci/sfx-intent-vs-real.py` 改**按 `prios` 5 级分档 + 用 `loudW`**；门限由
     "INFREQ−FREQ ≥ 2.0" 改为"**相邻档全部严格正向** 且 prio5−prio1 ≥ 2.0"。
     阴性对照 **2 → 4 条**，其中 **A 用例故意把高 prio 造得"短而响"、低 prio 造得"长而弱"**
     —— 旧全段口径必误报塌陷，新口径必 PASS，**这条即是"时长无关性"的回归守卫**。
  3. cardshow 真正该修的是"**过轻**"（30ms 窗口径 −16.4dB = prio3 档最轻、还低于 prio1 的 hit −15.3）。
- **⚠ 修复中的"双目标拉扯"（教训）**：抬响度与"别撞 start"互相冲突 ——
  - 先试 **加 sustain（s=0.25）** → RMS 0.1033 ✅ 但**结构同构率 0.571→0.857** ❌
    （sustain 正是 start 的特征，加了等于把谱形包络推向 start，双轨告警重新亮起）
  - ✅ 正解 = **两个杠杆分开用**：抬响度用**音数/节奏密度**（不用能量填充）。
    终版 = **高音区 4 音密簇** `784/1046/1318/1568 @ 0/.07/.14/.21`：
    RMS **0.1022**、对 start 结构同构率 **0.000**（7 维全不同）、重心带位 3.40（≈1055Hz）。
  - ⚠ 也别退回 v1.175 版：那是 `523/659/784/1046` **中音区级进**（重心 2.98），**是另一个东西**。
- **结果（v1.177）**：
  - 意图-实响：**5 档严格单调 ✅**（80.4 > 77.7 > 77.4 > 76.2 > 74.2）· prio5−prio1 = **6.2dB**
  - 双轨感知：`cardshow↔start` **完全跌出告警榜**（此前 d0.232/s0.571 → 现在不再进入前 12 对）
  - 三形态同步 · `sync-deploy` **必需缺失 0** · `final-audit` **PASS**（音频哈希 29/29 一致）
  - 云端 `verify-dist` run `35628253350` **success** + `audio-audit` run `35628886856` **success**
    （日志确认 `✅ 阴性对照全通过（A 时长无关 / B 塌陷可检 / C 覆盖度 / D 逆序可检）`）


## 🔒 关键约束（不可违反）

- **数值与密度冻结**（R7G 裁决）：只做**表现层**提升，不动数值 / 密度 / 开局节奏
- **预渲染裁决**（R2/R4）：状态贴图走**预渲染切换**，**禁逐帧染色 / 翻转**
- **体积闸门 = HTML 主包 ≤4MB**（微信小游戏口径；CDN 远程资源**不计入**包体）。
  ⚠ **2026-09-21 11:0x 实测复核**（此前记录有误，已按实测改）：
  | 形态 | 路径 | 体积 | 距 4MB 余量 |
  |---|---|---|---|
  | **内联母版**（贴图 base64） | `.workbuddy/v1.162/mengshou/game/萌兽消消岛.html` | **4,168,499 B = 3.98 MB** | **仅 ~25 KB** ⚠⚠ |
  | **发布物**（贴图外链） | `deploy/index.html` + `deploy/assets/`（185 个） | **1,684,973 B = 1.61 MB** | **2.4 MB** ✅ |
  | dist 中间物 | `.workbuddy/v1.162/mengshou/dist/` | 185 文件 / 3.60 MB | — |
  - ⚠ **结论：内联母版本地预览版已几乎贴线，绝不能再往内联版加贴图**；
    要加美术只能走 `dist → deploy` 外链路线（那里余 2.4MB）。
  - 画布总和 12MB / JS 堆 33MB 属**运行时内存**（小游戏 ~256MB 额度），**不设 4MB 线**，仅作参考。
  - 🗑 旧文字「像素内存 ≤4MB（当前 3.402MB）」**四个口径都对不上，已作废，不要再引用**。
- **别把"有意设计"当缺陷改**（**已连续踩 3 次**）：
  1. 战场「纯深蓝 + 单条土石路」是 v1.145→v1.152→v1.162 三代刻意打磨（注释：杂物移除，不抢英雄戏）
  2. 大厅「苔纸 + 细墨框」扁平是几十版刻意"去重金"的结果（v1.157：金不再作大厅主贴片色）
  3. 面板静止态扁平是有意的层级系统：16 处面色中 **9 处带状态分支**（按下变粗深 / 热变金 / 就绪变绿 / 暗降 alpha）
  - → **动视觉前，先在注释里找该处有没有版本裁决；批量改前，先统计该处有没有状态分支。**
- 产物不落 C 盘 · 验证截图属内部件不入交付 · 不出暴力血腥表现
- **改前先量数据**：任何"这是缺陷"的判断，必须先用运行时真实值验证前提

## 🚧 当前状态：客观指标全绿 + 美术已系统体检 → **只剩发布与方向决策**

```
敌人 21 · 受击帧 21/21 · 死亡帧 21/21 · 贴图条目 178
音频 29（10 BGM + 19 SFX）· **映射四方一致 19/19/19/19** · **fetch 覆盖 29/29**（v1.169 修）
音频（v1.170）· **19 个 SFX 峰值统一 0.7000**（双重缩放已除）· **档位 INFREQ>MID>FREQ，落差 +3.5 dB** ✅
音频（v1.170 续）· `bgm_win` 补低音层（<200Hz 5.8%→27.1%）· `sfx_win`/`sfx_lose` 补低音层（low 0.0%→~7%）✅
性能（v1.171）· **新增重载帧时门禁**：怪潮 47 怪同屏 p95 34.6ms / 36.4fps（软件渲染）· 阴性对照 max 281ms ✅
存档（v1.172）· **新增存档完整性门禁**：33/33 键 · 往返 ok · 篡改拒绝 3/3 · 钳制 11/11 · v1→v7 迁移旧值全留 ✅
门禁自审（v1.172）· **补「门禁收口」步骤**：verify/stress/save 三门禁此前 `continue-on-error` **FAIL 也显示 success** ✅已修
云端验证（2026-09-21 22:0x run 35609728401）：结论 PASS · 23 步全绿（新增存档体检 + 门禁收口）
云端验证（2026-09-21 21:3x run 35605956457）：结论 PASS · 6m3s · 21 步全绿（新增重载帧时体检）
美术体检（离线）：11 家族 / 209 SPRITES / 74 图标全量对照 → **真缺陷只有 1 个，已修**
deploy/ 自校验「必需缺失 0」✓  →  deploy/index.html + deploy/audio 29 个（v1.170 重同步）
```

### 🎨 本轮美术/可用性修复（2026-09-21，均已云端验证）

- **`gear_thornpaws`（荆爪套图标）过暗**：实测亮度 **34.1**，gear 家族 118.1±34.2 → **离群**；
  同主题兄弟 `gear_thornbow`=148.4、`gear_paws`=149.2 反证非刻意做暗 → `gain` 提到 **84.2**。
- **【v1.167g3】地表质感层**：战场一片 170×100 区域实测 **仅 22 色、最高频一色占 59.7%**；
  屏幕级纯色空格 26%、83% 像素对比度<2:1。成因：项目为"不抢英雄戏"**连续四代做减法**
  （`drawChapterFarLayer` v1.152/153/154/162）+ v1.162 砍掉星巅 6 团色斑。
  → 补**质感**不补**物体**：`stampGroundGrain()` 逐像素平滑 value noise（幅度 ±12 亮度级），
  烘焙进瓦片（运行期零成本、零新增资产），`CONFIG.groundGrain=0` 一键回退。
  → 颜色数 **22→173**、最高频一色占比 **59.7%→7.9%**、屏幕纯色空格 **26%→0%**，亮度分位几乎不变。
- **【v1.167】金色文字可读性**：章节页事件时间表用金色 `#c9a24b` 压浅面板（实测底色 `rgb(238,244,220)`）
  只有 **2.13:1**（正文需 ≥4.5:1）→ 新增 `c.cardGold = "#7a6220"`（**5.18:1**），**只改文字**，
  `#c9a24b` 画圆那处保留。
- **同轮修掉我自己的 bug**：v1.166 底色罩染是**逐帧 `createLinearGradient`**，
  违反 22663 行明文规则「禁逐帧创建渐变」→ 改预渲染 1×256 渐变条 + `drawImage`。

### 🔧 工具链修复：界面巡检长期「假成功」（同日，影响很大）

- **发现**：`screen-sweep` 长期打印"完成"，但 **13 张截图里只有 4 张不同** →
  **9 个界面从来没被巡检过**（此前记的"UI 巡检 10 界面"不成立）。
- **根因**：每屏只调一次 `open*` 就截图 → **前一个弹窗还开着时"打开"被守卫忽略**。
- **修法**：① 每屏前 `closeGear()+goHome()` 复位；② 截图算 SHA1，重复即标记并**非零退出**
  → 修复后 **13/13 真正不同**。
- **配套**：巡检视口 **1280×720 → 720×1280**（原会把画布缩到 405px，**细节全糊、无法评审美术**）。
- **第二层（更难发现）**：`codex`/`heroSheet`/`daily`/`levelup` 等钩子**是"状态查询"不是"打开动作"**
  → 换成真动作（`codexTab('enemy')` / `openSettings` / `dailyPick` / `levelupTap` / `pause(true)`）。
- **第三层**：**"与大厅不同" ≠ "是目标屏"**（`11-battle` 拍到过每日面板、`12-levelup` 拍到过普通战斗帧）
  → 给游戏补**通用状态口 `MENGSHOU_DEBUG.state()`** + 巡检**数据化断言表**
  （`EXPECT` 状态名 / `FLAGS` 弹窗标志 / 战斗·暂停硬断言）→ **断言失败即非零退出**。

### 🎨 美术拆成 6 部分（2026-09-21 定；逐部分按数据推进，不凭感觉）

| # | 部分 | 覆盖 | 量化手段 | 现状 |
|---|---|---|---|---|
| ① | **战场环境** | 地面瓦片 / 远景 / 氛围粒子 | `screen-audit.py` + `ground-compare.py` | ✅ 质感层（22→173 色）+ 土路压亮度（实测与英雄同亮 0.098~0.120 且占屏 26% → 降 25%） |
| ② | **单位** | 主角 / 21 敌人 / 7 跟宠（209 条目） | `art-metrics.py` 家族离群 | ✅ 1 个真缺陷已修（`gear_thornpaws` 34→84）；跟宠 7/7 有 AI 图 |
| ③ | **界面框架** | 13 屏面板 / 卡片 / 留白 | **`screen-profile.py` 弱度排序** | 🚧 **主攻中** — 面板"高得离谱"已修 3 屏：设置 317→191 · 关于 156→50 · **每日挑战 191→0** px |
| ④ | **图标与物品** | 74 图标 + 宝石 / 金币 | `art-metrics.py` + `art-intent-diff.py` | ✅ 家族内无离群（只有 thornpaws 一个，已修） |
| ⑤ | **文字可读性** | 全屏文字（截图采样对比度） | 从**渲染截图**采样底色算 WCAG | ✅ 金色文字 2.13→5.18；提示字 `#5c6844` 4.18→**4.70**（11 处） |
| ⑥ | **特效与反馈** | 受击帧 / 死亡帧 / 粒子 / 飘字 | **`ci/fx-audit.mjs`**（阴性对照 + 存活检测 + 计数口） | ✅ **全绿**：四项特效均可见 + **死亡帧/受击白闪计数口双证**（已为 0.07s 白闪补 `hitFlashSpawned/Drawn`） |
| ⑦ | **音频（"听到的"）** | BGM / 音效 / 静音三档 | **`ci/audio-audit.mjs`**（钩 WebAudio 音源创建计数） | ✅ **全绿**：三档 + **音质**（无削波 · RMS 0.10）+ **⑥循环接缝**（v1.167j 修正判据 + 裁尾修复） |
| ⑧ | **性能（"卡不卡"）** | 重载帧时（怪潮同屏） | **`ci/stress-perf.mjs`**（先证有怪 + 帧时尾部 + 阴性对照） | ✅ **全绿（v1.174 修假警报）**：45 怪同屏 · p95 **23.6ms**（预算 80）· 窗口 10s/528 帧 · 异常 0 ⚠ 预算原 50 时**约 1/5 概率随机判红**（同代码 p95 极差 2.8×） |
| ⑨ | **存档（"进度还在吗"）** | SAVE_SCHEMA / 迁移链 / 钳制 / 导出导入 | **`ci/save-integrity.mjs`**（四组断言 + 正/负对照） | ✅ **全绿（v1.172）**：33/33 键 · 往返 ok · 篡改拒绝 3/3 · 钳制 11/11 · v1→v7 旧值全留 |
| ⑩ | **门禁诚信（"检查器自己诚实吗"）** | 8 个 workflow 的门禁步骤声明 | **`ci/gate-audit.mjs`**（静态扫描 + 6 组对照 + 故障注入） | ✅ **全绿（v1.173）**：3 处"名不副实"已修 · 判据自带 6 组对照 · 给门禁自己打洞能被检出 |
| ⑪ | **文案覆盖率（"看得见的都写了吗"）** | 「淡刀子」系统在敌人图鉴 / 跟宠上的实际覆盖面 | **`ci/lore-coverage.mjs`**（静态解析 + 3 负 2 正对照）+ **`MENGSHOU_DEBUG.lorePack()`** 运行时证据口 | ✅ **全绿（v1.173）**：敌人 **5/21 → 21/21** · 跟宠 **2/7 → 7/7** · 运行时 `enemyInPack:21 / petInPack:7 / missing:[]` · pageerror 0 |
| ⑫ | **决策文案覆盖率（"选得出来吗"）** | `DATA_CODEX_FORK` 20 条进化分支的"选它当口" | **`ci/branch-pick.mjs`**（5 判据：覆盖/质量/透传/渲染/几何 + 4 负 2 正对照） | ✅ **全绿（v1.174）**：分支 **0/20 → 20/20** 有 pick · 消费+渲染+几何三侧自证 · 卡高 96 内不溢出 |
| ⑬ | **SFX 感知区分度（"听得出来吗"）** | 29 个音效 **171 对**的"是否可区分" | **`ci/sfx-distance.mjs`**（**双轨合议**：结构同构率 + 20 带感知距离 + 8 条自测） | ✅ **全绿（v1.175）**：win↔lose 结构同构率 **0.429 → 0** · 结构去同构人工对照 **86%→29%** · 自测 8/8 · 云端 `35624402620` success ⚠ 判据经**三次自我纠错**（8 带非单调 / 距离轨假阳 / 结构轨假阳）才立住 |
| ⑭ | **SFX 纵向层级（"该响的响吗"）** | 19 个 SFX 的 **5 级 prio 层级**（受击/复活 > BOSS > 结算/击杀 > 宝石 > 开火/命中） | **`ci/sfx-intent-vs-real.py`**（按 `CONFIG.audio.prios` 5 级分档 + **30ms 滑动窗峰值响度** + **4 条阴性对照**） | ✅ **全绿（v1.177）**：5 档严格单调（80.4 > 77.7 > 77.4 > 76.2 > 74.2）· prio5−prio1 = 6.2dB · 对照 A 为**"时长无关性"回归守卫** · 云端 `35628253350` success ⚠ **判据第三次犯同一尺度错**（全段 RMS 让时长混进响度；前两次＝v1.170 逐对 gain、v1.174 门限贴带） |

### 🔊 音频循环接缝（2026-09-21 15:xx，本轮最硬的一次排查）

**结论：6/6 全绿（云端 run 35575304684 PASS）**，但过程推翻了我自己两轮判断：

1. **我上一轮改错了对象**：v1.167i 给**程序化渲染** `_renderBgmTrack` 加 `bgmTailFade`，
   而项目有 **6 个外采 `game/audio/bgm_*.wav`**，`tryLoadLocalFiles()` 会 fetch 它们并
   `_overlayExternal()` **整个覆盖**程序化结果 → 我改的那条路径在 CI 里**没人走**。
   （CI 报的 16.271s 缓冲 = `bgm_march.wav`，与实测该文件时长**完全对上** → 反证。）
2. **CI 的 ⑥ 判据本身是假阳性**：阴性对照 —— 一个**数学上完美循环的正弦**被旧口径判 FAIL（0.361）。
   旧口径第二项取"任意样本对最大差"，44.1kHz 下相邻两样本跨半周期就能差 ~0.5。
3. **物理正确口径**（v1.167j 换上）：`seamJump = |d[0]-d[n-1]| ÷ 相邻样本差 P95`，阈值 <4×。
   **并内置 ⑥′ 判据自检**（完美循环须过 / 人为断崖须挂）→ 判据先证明自己有效才许下结论。
4. **真缺陷定位**：6 个 wav 尾部「降到静音后又回升一段后缀」（尾音尾巴），
   **接缝断崖点 = 后缀起点**（不在末端，所以"淡出尾部"完全无效，实测 38.86×→38.86×）。
5. **修法 = 裁掉末尾 60ms 后缀**（落到自然静音谷）：
   `city 38.9→0.1 · dune 42.5→0.01 · frost 5.1→0 · harbor 26.2→0.01 · march 2.3→0.01 · meadow 48.2→0.02`
   工具 `ci/trim-bgm-seam.mjs`（幂等 + 自动备份 `ci/audio-backup/`）。
   **两条 BGM 路径各自修对**：外采 wav = 裁尾；程序化 = `bgmTailFade`（离线仿真 0.000）。

**推进顺序依据**：③ 界面框架是当前最弱的一族（`screen-profile` 弱度榜前 4 名里 3 屏属它），
且这 3 屏**此前从未被真正巡检过**（工具"假成功"掩盖了它们）。
**方法纪律**：每部分都走「云端取 1:1 截图 → 我亲眼确认 → 量化定位 → 改 → 云端复验 + 改前后对比」。

**11 部分现状（2026-09-21 收口）**：① 战场 ✅ ② 单位 ✅ ③ 界面框架 ✅（设置 317→**101px** 空白带并修掉自查出的重叠）
④ 图标 ✅ ⑤ 文字 ✅ ⑥ 特效 ✅（含死亡帧/白闪计数口双证）⑦ 音频 ✅（含音质：无削波 + **循环接缝 6/6 全绿**）
⑧ **性能（"跑得动吗"）** ✅（v1.174 修**假警报**：预算 50→80 + 窗口 5s→10s；
  同代码 p95 实测极差 2.8×（18.3/21.1/29.9/50.6），50 卡在带上沿 → 约 1/5 概率随机判红）
⑨ **存档（"进度还在吗"）** ✅（v1.172 新增**存档完整性门禁**：结构/篡改/钳制/迁移四组 + 门禁自身有效性对照）
⑩ **门禁诚信（"检查器自己诚实吗"）** ✅（v1.173 新增**门禁自审**：声称门禁必须真拦人 ·
  修 3 处名不副实 · 判据自带 6 组对照 · 故障注入实证会拦人）
⑪ **文案覆盖率（"看得见的都写了吗"）** ✅（v1.173 新增**「淡刀子」覆盖率门禁**：
  敌人图鉴 **5/21→21/21**、跟宠 **2/7→7/7** · 补 21 条文案 · 运行时 `lorePack()` 双证 · 3 负 2 正对照）
⑫ **决策文案（"选得出来吗"）** ✅（v1.174 新增**「选它当口」覆盖率门禁**：
  `DATA_CODEX_FORK` 20 条分支 **0→20** 有 pick · 5 判据含透传/渲染/几何自证 · 4 负 2 正对照 ·
  顺带修 `codexContentBottom()` 漏算成档+进化两段的 pre-existing bug，少算 416px）
→ **客观层已反复确认干净**；后续若仍"不满意"，属**风格取向**，需要指定方向（或指定某一屏/某个元素）。

**⚠ 本轮三次"假象 / 自误"（都靠再量一次才拦住）**：
- 以为装备库面板外有 UI 露出 → 量"面板外亮度"五屏**完全一致**（0.143）＝遮罩正常
- 以为面板底色三屏不一致 → 多点采样确认 gear 真实底色 rgb(238,244,220) **与设置/关于完全相同**，
  先前采到的是**空格子的米色填充**（有意设计）
- **假阳性判据**：CI 接缝旧口径把"数学上完美循环的正弦"判 FAIL（0.361）→ 判据不成立；
  换物理正确口径并**内置阴性对照自检**（⑥′）后才许用它下结论
- **改错对象**：修程序化渲染去解一个由**外采 wav** 引发的接缝问题（那条路径运行时没人走）
  → 教训：改渲染器前先确认"**运行时真正走哪条路径**"（有无覆盖层）

### ⚠ 本轮否决掉的判断（省下无效改动）


- **地面图集缺失不是缺陷**：`chapterGroundSkipPng()` 对六主题全 true、`hallGroundSkipPng()` 恒 true
  → v1.96 **有意禁用 PNG 地面**，地面走 `paintChapterGround()` 程序化绘制。
- **图鉴里跟宠是灰剪影不是缺陷**：`unlocked ? SPRITES["beast_"+id] : SPRITES["beastsil_"+id]`
  → **未购买显示剪影是常规约定**（且 7/7 跟宠都有 AI 彩图）。
- **静态对比度审计不可用**：本游戏深色 HUD 浅字与浅色面板深字并存 → 固定底色假设出 **26 个假阳性**；
  "取最近前置 fillRect"又跨函数失效 → **对比度必须从渲染后的截图采样**。

### 🔬 结论：贴图层"没有缺陷"，但**屏幕层**有真问题（本轮最重要的方法论）

- **贴图层**（209 条目 / 74 图标）→ 只有 1 个真缺陷（已修）。被"绝对阈值"标出的灰白物体
  （绒毛武器 0.049 / 石头 0.059 / 磁铁 / 白绒羊 / 枯枝）**逐个核实后都是设计**。
- **屏幕层**（新增 `screen-audit.py`）→ 才抓到"空、平"：**"贴图好看" ≠ "画面好看"。**
- ⚠ **另一条教训**：颗粒 v1 用 `alpha=0.055`，实际亮度偏移仅 **1.8 级** →
  **像素统计全面变好（22→184 色）但肉眼完全看不见**（3bit 量化指标比眼睛灵敏）。
  → **"指标变好" ≠ "看着变好"；改美术必须"指标 + 目视"双证，且改代码前先离线试参。**
- **"改哪、往哪改"仍需用户的眼睛定**（我已找到"空"这条真线索并动手，方向是否对要你看）。

### 🔊 音频：内容从 8 个补齐到 25 个（本轮）

- **缺口**：HTML 声明 10 BGM + 15 SFX，`game/audio/` 只有 6 BGM + 2 SFX → 17 个走程序化兜底。
  其中 4 个基础设施脚本：`ci/render-missing-bgm.mjs`（复刻 `_tone` + 4 条曲目参数离线渲染）、
  `ci/render-missing-sfx.mjs`（复刻 `_tone`/`_noise`/`_renderOne`，13 个）、
  `ci/resample-audio-44100.mjs`（8 个 22050Hz → 44100Hz 统一，幂等）、
  `ci/audio-local-check.mjs`（离线全量体检，exit 1 on fail）。
- **零 API 成本**：用**游戏自己的合成器公式**离线渲染 → 与运行时同源、接缝天生 <0.1、
  CPU 秒级完成，不需要任何音频模型/外采素材。
- **结果**：25/25 就位。44100Hz / 16bit / mono，峰值 ≤0.711（无削波）、
  接缝回跳 0.02~0.21×（阈值 4）、RMS 中位 0.0989。**云端 `audio-audit` PASS**。

### 📦 发布形态"没有音频"这个更严重的结构问题（本轮抓到并修）

- **现象**：`dist/assets` / `deploy/assets` 只有 178 个 webp，**一个音频都没有**；
  `ci/build-dist.js`（CI 用的那版）与 `_qc/sync-deploy.js` 都不处理 `audio/`。
- **根因（值得记）**：HTML 里音频文件名是**运行时按表拼接**的
  （`AUDIO_ASSET.baseDir` + `audioAssetFileName()`），**没有任何字面量引用** →
  构建脚本"看不到"、正则扫描也扫不出来 → **静默降级到程序化合成，外采素材全部白做**。
  而 `verify-dist` 只验 HTML 能否跑 → **验证全绿，问题是"没做"而不是"做错了"**。
- **修法**：音频外置逻辑**合并进 `ci/build-dist.js`**（单一真身，删除重复的 `_qc/build-dist-audio.js`）
  + 新增 **`ci/assert-dist-audio.mjs`**（数量 / 逐字节大小 / 源目录三方比对，缺失 exit 1）
  + 接进 `verify-dist.yml` 作为独立门禁步骤。
- **门禁做了阴性对照**（三类全 FAIL、阳性 PASS，见下）：
  | 样本 | 期望 | 实测 |
  |---|---|---|
  | 真实构建 | PASS | PASS ✅ |
  | 删整个 `dist/audio/` | FAIL | FAIL ✅ |
  | 只删 3 个文件 | FAIL | FAIL ✅（"有 22 个，声明 25 个"）|
  | 某个文件写成 100 字节 | FAIL | FAIL ✅（"源8864B vs 包100B"）|

### 🕳 本轮环境坑（新发现，与"验证无效"同源）

- **Bash 的 `/tmp` 与 node 的 `/tmp` 不是同一物理位置**：
  `bash rm -rf /tmp/x` 报成功但 node `fs.existsSync('/tmp/x')` 仍为 true →
  **阴性对照第一次"通过"是假的**（我删了"影子文件"，被测对象没动）。
  → 教训：**构造阴性样本必须用与被测代码相同的运行时**（这里用 node 删），
  否则你测的是"另一个目录"，会把有效门禁误判为失效、或反之。
- 顺带确认：**永远通过的门禁比没有更危险**——本轮断言若没做阴性对照，
  它会在 `dist/audio` 缺失时照样输出"PASS"，比不写更糟（虚假安全感）。

### 🐾 跟宠图鉴"全是灰剪影"——追查结论：**是设计，不是缺陷**（本轮第二处"看似缺陷"）

**现象**：图鉴"跟宠"页 7 张卡片全是灰剪影，但每张都带「购买 NNN 金」按钮 →
第一眼像"玩家看不到要买的是什么"的严重可用性问题。

**追查过程（三次口径修正，每次都是我自己错了）**：
1. 先查 AI 表 `beast_*` 前缀键 → **0 个** → 误判"素材从没做过"
2. 再查发现 `dist/assets` 里 `capybara.webp` ~ `veloursheep.webp` **7 个全在** → 改判"素材做了没注入"
3. 打出**全部 178 个键名**才发现：`capybara, bunny, ursa, zapmouse, oinkpig, shellturtle, veloursheep`
   **7 个键本来就在表里**（无前缀，第 108~114 位），且是完好的 base64 内联
   → 我第 1 步的 `/^beast_/` 过滤是**错的**（键名本来就不带前缀）

**真正的原因**（读代码得到，不是猜）：
- `skipActorPng()` **硬编码 `return true`**，注释写明：
  `【v1.96】单位/跟宠不再盖插画 PNG 或图集, 局内大厅同一套墨线剪影`
  → 这是 **v1.96「统一墨线+填色+剪影」** 风格决定的一部分（同批还删了金环/薄荷环/奶油卡/散斑噪声）
- 但 `makePetSprite()` 里另有 `if (id && AI_ART_READY[id])` **直接读缓存**的分支
  → 跟宠 AI 图**仍然是活的**，只是不走 `applyTexture` 那条路
- 图鉴卡片用的是 `SPRITES["beastsil_"+id]`（**解锁前**剪影）；`isBeastUnlocked` 只读
  `saveData.beastUnlocked` → 巡检用**全新存档** → 7/7 未解锁 → 7/7 剪影

**结论**：剪影是"未解锁"的**常规表达**，且与 v1.96 的风格统一一致。
**不是缺陷，不改**。真正该改的是**产品层**（"未解锁是否该给个更可读的预览"），
这属于主观取舍，**需要用户拍板**，我不擅自反转 v1.96。

> **本轮方法论追加**：一个"看起来明显是 bug"的现象，背后可能是**刻意的设计决定**，
> 且代码里有注释写明。**先读代码与注释，再判缺陷**——本轮我连错两次口径
> （前缀过滤错 → 误判素材没做），都是因为**没先看全量数据**就下结论。
> 判定顺序应是：**打全量键名 → 读渲染路径 → 读设计注释 → 才判**。

### 📋 内容缺口全维度扫描 —— 4 版返工，结论「无缺口」（2026-09-21 17:xx）

**动机**：把"该有的东西在不在"从**逐个猜**变成**一次全枚举**（12 个维度）。
背景是踩过"dist 缺 25 个音频而验证全绿"的静默缺失坑。

**扫描器返工 4 版，四次假信号全部是扫描器自身 bug，不是游戏的缺口**：

| 版本 | 做法 | 假信号 | 真因 |
|---|---|---|---|
| v1 | `indexOf('];')` 截断 | "章节 12 条含 barkplate"、"武器声明 10 vs 30 键" | 对象 map 的 `];` 在 11KB 外 → 串入兄弟数据 |
| v2 | 配平取体 + 只取顶层键 | 敌人 21 → 只数到 6 | **两层 map**：顶层是种族分组，变体在分组内 |
| v3 | 配平 + 递归收 id | BGM 只数到 1 | 顶层键正则要求键后紧跟 `{`，**单行条目**漏掉 |
| v4 | 配平 + **剥 `//` 注释** + 递归收 id | — | `//` 行注释被带进下一个键 → 键名脏 → 正则拒收 |

**关键结构知识（写进 README，免得下次再返工）**：
- `DATA_ENEMY` / `DATA_WEAPON` = **两层 map**（分组 → 变体数组）
- `DATA_BGM` = **带 `//` 行注释的单行条目 map**（`lose: { ..., jingle: true }`）
- `DATA_BEAST` / `DATA_GEAR` / `DATA_CHAPTER` / `DATA_WORLD_THEME` = 数组

**最终结论（v4 实测，全部 ✅ 零缺口）**：
敌人 21/21/21 · 跟宠 7/7 · 武器 10/10 `wpn_` · 装备 12/12 · BGM 10/10 · SFX 15/15
> 唯一"看似缺口"：3 个武器无 `blt_` —— 实测 `kind: strike/laser/nova` 且
> `bulletSpd:0, bulletR:0`，是**无弹道技能武器**（落雷/激光/光环），
> 不存在飞行子弹实体，**没有弹体贴图是设计如此**。

**沉淀**：
- 扫描器入库为 `ci/content-gap-scan.mjs`（含 `GATE=1` 门禁模式 + `--` 阴性对照）
- **接进 `verify-dist` CI**（每次 push 跑，有缺口即 FAIL）
- **阴性对照已做**：`ci/_negcontent-check.mjs` 造一个必然缺 8 类的最小样本 →
  实测 `exit=1` ✅（守卫真会 FAIL，不是装饰）
- 云端验证：run `35583273953` **PASS**（新门禁步骤在真实 CI 里跑通）

### 📖 README 重写：把散落的构建知识收进一份索引（2026-09-21 17:xx）

**发现的问题（客观可判，非主观）**：`README.md` 只写了 1 条流水线（`verify.yml`），
而实际有 **8 条**；且体积数据**过期**（写"当前 3.402MB"，实际 4.00MB）。
`ci/` 下已有 **20 个脚本**，无索引 —— 这正是"dist 缺音频没人发现"的同类风险：
**没人知道已经守住了什么**。

**重写内容**（全部数字取自实测）：
- 目录结构与**三形态同步**关系（母版手改 / dist 构建 / deploy 同步）
- **8 条流水线清单**（触发条件 / 跑什么 / 守住什么）
- **ci/ 脚本索引**（验证类 / 体检类 / 构建类，20 个脚本逐个说明）
- **内容清单**（v4 扫描实测表）+ 结构坑警示
- 体积口径（内联 4.00MB 已临界 / dist 10.21MB）

- **⏳ 未完成 1：部署上线**。线上站点仍是旧版（含白方块、无受击/死亡帧、荆爪套仍过暗、BGM 循环断崖、无外采音频）。
  **发布需用户当轮明确说「发布」**（防误覆盖已被分享的链接）。
  → 注：**此前"未完成1"里列的音频问题，本轮已全部在代码/资源层修好**，
  只等发布动作本身。
- **⏳ 未完成 2：美术方向需用户指认**（客观指标已全绿，剩下是主观取舍）：
  - **A**（**推荐默认**）保持现有语言，只补真正缺失的（空白区 / 弱层级）—— 风险最低
  - **B** 换更饱满风格：加纹理 / 体积 / 插画 —— **明确反转 v1.157 系列裁决**，风险最高
  - **C** 指认某一屏（13 张巡检图在 `_qc/sweep/`，总览 `_qc/sweep/全界面总览.png`）
  - 说明：**内联母版已贴 4MB 线**，B 路线只能走 `dist → deploy` 外链（那条余 2.4MB）

- **⚠ 本地与远端不同步（2026-09-21 11:0x 核查）**：`.workbuddy/v1.162/mengshou/` 这个 git 仓库
  只提交了 5 次，**今天的工作（新 workflow、`ci/*` 新脚本、`dist/`、游戏 HTML 改动）基本未进本地 git** ——
  因为我的推送走 **GitHub API（Git Data API）直接改远端**，不经过本地索引。
  → **本地 `git status` 看着"很多未提交"是正常的**，不代表没推上去；核对远端请用 `gh api`。

## 🔬 本轮四条「可判定/不可判定」判据总账（2026-09-21 18:xx 收口）

动机：把"音频/文字配平得好不好"从**感觉**变成**可复算的数**。
四条判据里 **1 条被自己证伪、3 条立住**；三例同型错误的根因都是
「**参考量与实际信号不是同一个尺度**」。

| # | 判据 | 脚本 | 结论 | 硬证据 |
|---|---|---|---|---|
| 1 | 文字可读性（屏幕层） | `ci/text-legibility.py` | ⛔ **证伪，不可采信** | 同屏对照：真文字块 95.4% vs **空白面板块 98.7%**（分辨不出）；整屏最暗像素偏移 1px → 对比度 **1.03 → 19.42** |
| 2 | BGM 循环接缝 | `ci/loop-seam.py` | ✅ **立住**（已作门禁） | 23 循环曲全 PASS，最高 `bgm_city` ratio 0.20；阴性对照三次构造才成立 |
| 3 | BGM 频谱重心 / 编排平衡 | `ci/bgm-spectrum.py` | ✅ **立住** | 中位重心 **225Hz**，稳健σ 123Hz；9 首 OK + 1 可解释离群（`bgm_win` 上行 jingle 1120Hz） |
| 4 | SFX 响度层级 | `ci/sfx-loudness.py` | ✅ **立住** | **INFREQ −17.3 > MID −22.0 > FREQ −22.6**，低频事件比高频高 **5.3 dB**，组内无离群 |

### 案例 1：文字可读性——判据被自己的实验推翻（保留痕迹，不删）

- **原口径**：缩略图里找"近白文字像素"→ 取 5×5 邻域众数当底色 → 算 WCAG 对比度。
- **证伪一**：**67.3%** 的"近白文字像素"其邻域众数背景**也是近白** —— 它们长在
  奶白面板底 `#eef4dc` 上，是**无字底纹**，不是文字。
- **证伪二（决定性）**：缩略图 300px = 原 720px 的 **0.4167×**，笔画只剩 **2–5px**
  → **5×5 窗口比笔画还宽**，众数是**随机量**。窗口偏 1px，同一像素的"底色"
  从 `(11,13,10)` 跳到 `(248,254,230)`。
- **连带发现**：`screen-analyze.py` 的 `lowct%` 常年 **70–91%** 是**同源虚高**，
  **不该当缺陷追**（此前两次把它当成"文字可读性差"的证据，方向是错的）。
- **处置**：加证伪横幅 + report 标「本表不可采信」+ CI 步骤降级 `continue-on-error`
  + **保留文件**（`--selftest` 继续作回归，但注明"合成自测有盲区"）。

### 案例 2：BGM 接缝——阴性对照三次才立住（最难的一次）

- **v1 判据错**：参考量用**全曲 P95 逐样本差** → 那度量的是**高频含量**。
  440Hz 正弦逐样本差恒为 ~1250 → 任何接缝都被"自己的高频"掩盖（ratio≈1.0）
  → **曲子越亮越测不出接缝**，对打击乐/高频 BGM 完全失效。
- **v2**：`ratio = |首−末| ÷ P90(首尾各 128 样本逐样本差)`，阈值 4.0。
- **阴性样本三次构造**（每次失败都改变了我对问题的理解）：
  1. 正弦首尾**反相** → ratio 3.00 判不出。**错因：正弦天然无缝，反相也接得上。**
  2. 60Hz 取各相位 → 全部 ≈1.00。同上。
  3. **带包络的曲被强行截断**（末段淡出到非零 / 残留直流）→ 成立：
     A 尾淡到 0 → **0.00** ✅不误报 ｜ B 尾停在 +9000 → **39.11** ✅ ｜ C 末 50 样本残留直流 → **52.17** ✅
- **实测**：25 wav（23 循环 + 2 一次性）接缝全 PASS；`ONESHOT` 曲标 `N/A(一次性曲)`。

### 案例 3：BGM 频谱——绝对阈值"恒真"（与案例 1 同型）

- **v1 错**：用「高频(2k–6k)+空气(6k–20k) 占总能量 > 4%」判"不闷"→ 25 条 **24 条判闷**。
- **真因**：本项目合成只用 sine / triangle / saw，实测**波形固有频谱**：

  | 波形 220Hz | low | high | air |
  |---|---|---|---|
  | sine | 100.0% | **0.0%** | **0.0%** |
  | triangle | 98.5% | **0.0%** | **0.0%** |
  | saw | 60.8% | 4.3% | 1.9% |

  → 对 sine/triangle 垫乐，`air < 4%` **恒成立**。该阈值测的是"用了哪种波形"，
  **不是"配平好不好"**。**恒真的阈值 = 在报定义，不是在测量。**
- **v2**：`centroid`（能量加权平均频率，**相对量**：单音时恰等于基音，多音落在编排重心）
  + `lowShare` **只做跨曲横向比较** + **中位数 & MAD** 稳健离群（3×1.4826×MAD）。
- **自测**：sine 220→**220.0Hz** ｜ 880→**880.0Hz** ｜ saw 220→**737.8Hz**（能分辨谐波）｜ 静音→安全 None。

### 案例 4：SFX 响度——能量口径，附带一条新规律

- **要回答的**：15 个音效"谁该压住谁"。伤"高级感"的不是单个音效，而是**相对层级**：
  高频事件（开火/命中，每秒数次）必须显著低于低频事件（升级/结算，每局几次），
  否则长玩被磨耳朵；但也不该低到"没反馈"。
- **口径**：**简化 K 加权能量响度**（一阶高通 100Hz + 一阶低通 8kHz + 均方 → `10log10`），
  **不是峰值**。⚠ 明确声明：**不是标准 LUFS**，只是**本项目内部横向可比**的相对值。
- **自测四项**：A 振幅减半 → **6.02dB**（期望≈6）｜ B 时长减半 → −10.6 vs −7.5 ｜
  **C 峰值相同（尖脉冲 vs 持续音）→ −48.7 vs −4.0，差 44.7dB** ｜ D 静音 → `-inf` 不崩。
- **🆕 新规律（已写进技能）**：任何"响度/能量"类判据，
  **必须构造一对「峰值相同、能量不同」的样本** —— 否则一个"其实在测峰值"的错误实现
  **会全绿通过自测**。（同型：判"亮度"要同均值不同分布；判"模糊"要同对比度不同高频。）

### 四条通用铁律（沉淀到技能 `ai-art-pipeline` 第十·四节）

1. **参考量必须与"被测现象"同尺度** —— 否则测的是别的东西（案例 1、2、3 全是这一条）。
2. **合成自测通过 ≠ 判据可用** —— 必须拉**真实产物**跑；三项自测全绿的判据照样可能在真图上失效。
3. **阴性样本的构造方式本身就是一次证伪** —— 先问"这个样本物理上真含该现象吗"
   （正弦反相 ≠ 接缝；sine 高频为 0 → 测不出高频）。
4. **绝对阈值要对齐信号固有属性**，否则恒真 —— 恒真的阈值 = **在报定义**，不是在测量。
5. （本轮新增）**能量类判据必须含"峰值相同、能量不同"的对照。**

> **⚠ 这四条是我自己踩出来的，不是从书上抄的**：本轮同一型错误出现 **3 次**
> （content-gap-scan 解析器 / text-legibility 邻域众数 / bgm-spectrum 绝对阈值），
> **全部靠真实产物上的对照实验才抓到**，靠"再想一遍"一次都没抓到。

## 🧰 复用要点（省得重查）

- **调试接口** `MENGSHOU_DEBUG`（全局）：`readField()` → 场上实体计数；`guide()` → 引导状态；
  另有 `spawnElite`/`spawnBoss`/`bomb`。**判断内容一律读它，不要靠截图数数。**
- **采样注意**：升级三选一弹窗会暂停游戏（`GAME.runTime` 冻结）→ 连续采样前先关弹窗。
- 可复用美术管线（环境变量驱动，`_hit` / `_dead` 各跑一趟）：
  `ci/dead-frames/*_raw.jpg` → `ci/build-dead-frames.py`（`ART_SUFFIX`/`ART_SIZE`/`ART_IDS`）
  → `ci/patch-death-wiring.mjs`（6 处接线）→ `ci/verify.mjs`（双通道 + 证据口）→ workflow `death-frames.yml`
- 工具链 31+ 脚本；改完跑 `script_lint`（应 0 error 0 warn）。
- 定时任务提示词：`.workbuddy\定时任务-提示词.md`
- scope：主改 `.workbuddy\v1.162\mengshou\game\萌兽消消岛.html`（配套 `ci/`、`_qc/`、`dist/`、`deploy/`）
