# AGENTS.md — 给 AI 编码代理的项目须知

> 本文件由一次代理会话于 2026-09-23 依据**仓库内实测**写成，不是凭印象。
> 每条都标了依据位置；与 `GOAL.md` 冲突时以 `GOAL.md` 为准。

## 一、这是什么

单文件 HTML5 游戏《**萌兽不好惹**》（竖屏 9:16 幸存者类）。
历史别名：萌兽消消岛（"消消"易被误认为三消）、萌兽幸存者（撞句式）——**均不要再作为玩家可见名使用**。

**三形态产物**，改动前先想清楚动的是哪一个：

| 形态 | 路径 | 作用 | 体积闸门 |
|---|---|---|---|
| 内联母版 | `game/萌兽消消岛.html` | 本地双击预览（贴图 base64） | 超 4MB **仅 warn** |
| dist 中间物 | `dist/萌兽消消岛.html` | 贴图外链中间产物 | **≤4MB 硬门禁** |
| 发布物 | `deploy/index.html` + `deploy/assets/` | 唯一可发布形态 | 同上 |

依据：`ci/final-audit.mjs` L148（dist >4MB → `fail`）/ L149（inline >4MB → `warn`，注释原文"仅本地双击用，不影响发布"）。

## 二、不可违反的约束

（依据：`GOAL.md` L448–469）

1. **数值与密度冻结**（R7G 裁决）——只做表现层提升，不动数值 / 密度 / 开局节奏。
2. **预渲染裁决**（R2/R4）——状态贴图走预渲染切换，**禁逐帧染色 / 翻转**；另有明文规则**禁逐帧创建渐变**（应预渲染 1×N 渐变条 + `drawImage`）。
3. **体积闸门**——硬线在 `dist` 主包 ≤4MB。内联母版过线是**已知且被接受**的状态，不要"修复"它。
4. **别把"有意设计"当缺陷改**（**已连续踩 3 次**）：
   - 战场「纯深蓝 + 单条土石路」= v1.145→v1.152→v1.162 三代刻意做减法
   - 大厅「苔纸 + 细墨框」扁平 = 几十版刻意"去重金"
   - 面板静止态扁平 = 有意的层级系统（16 处面色中 9 处带状态分支）
   - → **动视觉前，先在注释里找该处有没有版本裁决；批量改前，先统计该处有没有状态分支。**
5. **产物不落 C 盘** · 验证截图属内部件不入交付 · 不出暴力血腥表现。
6. **改前先量数据**——任何"这是缺陷"的判断，必须先用运行时真实值验证前提。
7. **命名红线**：任何名字**不可含「魔兽」二字**（商标雷区）。

## 三、音频资产（实测）

- `game/audio/`：**29 个 `.wav`** = 10 BGM + 19 SFX；格式统一 **PCM / 16-bit / 单声道 / 44100 Hz**。
- **19 个 SFX 峰值统一 0.7000**（已用纯 Node 分析器复核，逐文件一致）；BGM 峰值 0.708–0.7192。
- 无裁剪样本（clipped samples = 0）。
- ⚠ `GOAL.md` 里记录的频段占比（如 `sfx_win` low ≈7%）所依据的测量脚本**不在本仓库**，无法复现其定义；引用那些数字前先确认方法。

## 四、验证链路

- 公开仓库 → **GitHub Actions 分钟数免费不限**，14 个 workflow 已承担重活：
  `verify.yml`（push 到 main 自动跑，36 步）、`verify-dist.yml`、`screen-sweep.yml`、
  `bgm-melody.yml`、`bgm-lane-loudness.yml`、`death-frames.yml`、`art-audit.yml`、`audio-audit.yml` 等。
- **提交即触发云端验证**——本地不必重复跑重门禁。
- `ci/` 下 **58 个 `.mjs`**（连 `.py`/`.js`/输出件共 **77 个文件**），其中 **32 个**支持 `--selftest`（阴性对照）；**自测不过就不该上岗**。
  ⚠ **口径（第 69 轮实测复核；本行数字就是当轮实测值）**：脚本数随门禁增长而变，**引用前先数一遍**（别沿用文档里的旧数字）——2026-09-24 实测：58/77/32。

## 五、代理本地可用工具

见工作区 `_dsh-kit/`（不在本仓库内）：

- `wav-analyze.mjs` —— 纯 Node WAV 分析（峰值 / RMS / 时长 / 裁剪 / 频段占比），无需 ffmpeg。
- `fix-profile-bundles.ps1` —— 维护用，与本仓库无关。

## 六、写代码前先确认

1. 这个改动属于哪一形态（内联 / dist / deploy）？
2. 它有没有历史版本裁决注释？
3. 它会不会把某个"有意设计"当缺陷改掉？
4. 判断依据是**量出来的**还是**看出来的**？
