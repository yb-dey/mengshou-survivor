# ART-SYSTEM.md —— 《萌兽不好惹》视觉系统科学规范

> 用户 2026-09-26 当面纠正：「美术不只是四个界面，是**整个 HTML 文件的所有内容美术**；
> 先了解科学知识和美术知识，再判断。」
> 本文件把美术判断的**科学依据**写成一页纸，之后每一批视觉改动、每一次"眼睛"复查都以它为标尺。
> 玩法对标弹壳特工队，美术对标元素骑士；数值/密度/节奏冻结（GOAL.md L448–469）。

## 一、五条科学原理（判断的依据，不是口味）

1. **明度先行**：任何"好不好看"先看**灰度图**。明度差 = 层次，色相差 = 情绪。两张色块若明度相等，
   无论色相多不同，灰度图里就是一块。改色前先问：这一处的新色在灰度图里和邻居差几档？
2. **60-30-10 色彩配比**：一屏 60% 主色（底）、30% 辅色（卡片/面板）、10% 点缀色（按钮/关键数字/光）。
   点缀色只给"玩家 0.5 秒内必须看到的东西"。现在的问题就是满屏都是 10% 级的颜色在互抢。
3. **冷暖对冲制造奇幻**：冷底（蓝紫）+ 暖物（奶油/金）才成立；同色相深浅变化 = "闷"。
4. **五级抬高系统**（元素骑士/弹壳共同语言）：底（场景）→ 静（嵌在底上的格）→ 抬（卡片）→
   浮（模态/主按钮）→ 最高（爆点：胜利标题/大数字）。每升一级多一套"凸起四件套"：
   外深描边 + 顶部内高光 + 底部硬投影（offset 实心）+ 内阴影。**投影色必须比落面暗两个明度阶**，
   否则等于把影子画在自己的颜色里（已踩）。
5. **光的三层**：发光 = 中心白核 + 彩色辉光 + 放射线/粒子，缺一层就是"亮"不是"爆"（已踩）。

## 二、落到本作的具体令牌（改动只用这些，不再拍脑袋）

- 冷底：#1f2444（结算）/ #262b4a（深盘）/ 大厅深靛蓝罩 rgba(22,26,60,.76)
- 暖物：奶油卡 #f4efe0~#e3ecd0；金 #ffd98a→#d99a2b；品质五色 = DATA_GEAR_RARITY 现有值
- 主按钮绿渐变 #7be86a→#2fb84a（预渲染 strip）；主次按钮：每屏**只允许一个**主按钮
- 文字三级：主 22–28 / 次 16–18 / 微标签 13–14；正文对比 ≥4.5:1
- 凸起四件套画法：`rgba(0,0,0,.16)` 底暗边 3px + `rgba(255,255,255,.30)` 顶高光 2px +
  `#120b2a` 硬影 6px + 品质色描边 3px
- 爆点光画法：预渲染 520×280（中心 #ffcb4c 85% → #ff7c1f 35% → 0）+ 12 条 3px 射线

## 三、全游戏美术面清单（"整个 HTML"的完整地图 —— 逐项过，不许只盯四屏）

| 区域 | 画面（对应巡检截图） | 已做 | 待判 |
|---|---|---|---|
| 大厅系 | 01-home / 02-chapters / 04-upgrade / 05-vault / 05b-vault-talent / 06-beast | 冷底、面板物理、图标统一 | 逐屏过 |
| 战斗系 | 11a-entrance / 11-battle / 12-levelup / 13-pause / 14-result | 弹窗穿帮、三选一、结算 | **战场内容美术全没动** |
| 文档系 | 07-codex×3 / 08-settings / 09-about / 10b-guide | 深盘统一 | 逐屏过 |
| **内容美术** | 战场地面/道路、怪物剪影、英雄、武器/道具图、箱子/掉落、命中特效、横幅、浮字 | **零改动** | 最欠账 |

## 四、工作方式（与用户商定）

Pro（我）动手；Flash（能看图的模型）只当眼睛，按本文件逐屏打分；
每批：改 → 扫屏 18 断言 → 发对比图 → 复查。数值一字不动。
